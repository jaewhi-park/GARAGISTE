---
description: Legacy assessment from the approved brief — inventory, risks, rebuild strategy (ADR), parity harness plan for the first seam. Run /brainstorm first; rebuild only after the harness exists
agent: team-lead
---
Assess an existing codebase and set a rebuild/modernization strategy. Target and context: $ARGUMENTS (when no path is given, the brief's Kind line names it)

0. Precondition: docs/BRIEF.md with `Status: approved` or `revised` and `Kind: 레거시 (<path>)` (from the injected board's Brief line, else grep those two lines — the one file read allowed here) — the brief holds why the rebuild happens, the target state, what must not change and the deadline. If there is none, or it is still whiteboard or draft, say so in one sentence — "run `/brainstorm <why, target state, what must not change, deadline>` or `/brainstorm <path of a document you wrote>` first" — and stop; with `Kind: 신규`, say so and suggest `/kickoff` instead. If AGENTS.md has no "## Language" section (a brief committed from elsewhere), ask the CEO the working language with the question tool (options: ko / en / other; default: the language of the brief), run `node .opencode/scripts/set-language.mjs <code>`, and continue in that language.
1. Load the `legacy-assessment` skill. Send explore out per area in parallel to gather the inventory, and have team-planner compile docs/ASSESSMENT.md — pass the explore reports to it verbatim, do not re-summarize, and name docs/BRIEF.md so it marks every "looks like a bug" item that conflicts with the brief's target state or invariants.
2. Report unknowns in chat, then get the preserve/fix policy in one question-tool call (split only if the tool limits questions per call): the question names each "looks like a bug" item with the planner's default (preserve, per the `parity-harness` skill) and offers "accept all defaults" first and "decide per item" second (then one question per item in follow-up calls); items the planner marked as conflicting with the brief get their own question. The answers go to team-planner verbatim in step 3 and are logged in docs/DECISIONS.md there.
3. Have team-planner compare strategies (strangler fig / module-by-module replacement / full rewrite) with a recommendation and the first seam to replace, written directly as docs/adr/0001-rebuild-strategy.md with `Status: proposed`, and in the same call derive docs/CHARTER.md from docs/BRIEF.md and the assessment (charter skill template, under 60 lines, `Source: docs/BRIEF.md`: Problem, Users and Constraints from the brief; Success metrics = parity PASS for every rebuilt seam plus the brief's success metric; Non-goals = the brief's non-goals, the rejected strategies and the seams out of scope; the preserve/fix decisions under Constraints; other sections `undecided — at the first /retro`); have team-critic review the ADR (on REVISE, findings verbatim to the planner, max 2 rounds, per the critic loop), then ask the CEO in one question-tool call to confirm the strategy, the charter's goal, non-goals and success metric, and each of the brief's Undecided items with the planner's default (edits and decisions go to the planner verbatim). Then one team-planner call sets the ADR to `Status: accepted` with the CEO's choice, applies the charter edits, records the Undecided decisions in docs/BRIEF.md and docs/DECISIONS.md, and writes docs/REBUILD_PLAN.md (step 0 of every rebuild step: extend the parity harness to that step's seam) and AGENTS.md per the `agents-md` skill ("## Commands" = the build/test commands the inventory found, each flagged `unverified`).
4. Have team-verifier run the Commands section. Then one team-planner call (pass the verifier's per-command results verbatim) removes the `unverified` marker from what passed and drops what failed (a command the guardrail blocked stays marked — tell the CEO its tool must be added to the verifier allow-list by a human), and writes docs/plans/0001-parity-harness-<seam>.md — the harness for the first seam per the `parity-harness` skill, whose last step registers the runner under Commands (do not invoke `/plan`). Have team-critic review that plan past the usual threshold (3+ logic steps or risk:high; on REVISE, findings verbatim to the planner, max 2 rounds, per the critic loop), then request approval with a summary (goal, step count, risks, expected diff) — /run takes only approved plans.
5. Have team-implementer commit the files this command wrote, by path — docs/ASSESSMENT.md, docs/CHARTER.md, docs/adr/0001-rebuild-strategy.md, docs/REBUILD_PLAN.md, docs/plans/0001-parity-harness-<seam>.md, docs/DECISIONS.md, docs/BRIEF.md if changed, AGENTS.md, docs/README.md, .gitignore whenever it is untracked or its `docs/STATUS.md` line is uncommitted (add the line when the installer did not), and .opencode/ and opencode.json if not yet tracked — on main (if the legacy checkout is on another branch, ask the CEO first), as `docs(assess): assessment, charter, rebuild ADR, rebuild plan, plan 0001`; the legacy tree's other uncommitted changes stay untouched. Write docs/STATUS.md (Now: none · Next actions: 1. `/hire` in this session 2. `/run docs/plans/0001-parity-harness-<seam>.md` in a new session). End by suggesting `/hire` to the CEO in a sentence — a rebuild weights critic and reviewer heavily, so assign in that direction; the brief says how much rides on it — and then a new session with that `/run` (model assignments load at session start) — no /handoff needed. No rebuild step starts before that plan ships with the harness PASS against legacy.
