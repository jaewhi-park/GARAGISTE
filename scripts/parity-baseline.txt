# GARAGISTE parity baseline — the accepted divergence between the claude and opencode flavors, after normalization
# (scripts/parity-check.mjs rewrites the claude side into opencode vocabulary first). Written by
# `node scripts/parity-check.mjs --update`; do not edit by hand. A `git diff` of this file shows which divergent
# lines a change touched. Content lines are word-diffs, claude [-removed-] and opencode {+added+}; structure lines
# compare a doc with its .ko.md translation per section; coverage lists files with no counterpart.

coverage
  claude-only: claude/.claude/agents/team-builder.md

claude/.claude/skills/assess/SKILL.md ↔ opencode/commands/assess.md
  5. Have team-implementer commit the files this command wrote, by path — docs/ASSESSMENT.md, docs/CHARTER.md, docs/adr/0001-rebuild-strategy.md, docs/REBUILD_PLAN.md, docs/plans/0001-parity-harness-<seam>.md, docs/DECISIONS.md, docs/BRIEF.md if changed, AGENTS.md, docs/README.md, .gitignore whenever it is untracked or its `docs/STATUS.md` line is uncommitted (add the line when the installer did not), and .opencode/ and opencode.json if not yet tracked — on main (if the legacy checkout is on another branch, ask the CEO first), as `docs(assess): assessment, charter, rebuild ADR, rebuild plan, plan 0001`; the legacy tree's other uncommitted changes stay untouched. [-Run `hire` via-] {+write docs/STATUS.md (Now: none · Next actions: 1. `/hire` in this session 2. `/run docs/plans/0001-parity-harness-<seam>.md` in a new session). End by suggesting `/hire` to+} the [-Skill tool (applied and committed after approval)-] {+CEO in a sentence+} — a rebuild weights critic and reviewer heavily, so assign in that direction; the brief says how much rides on [-it. write docs/STATUS.md (Now: none · Next actions: 1. `/run docs/plans/0001-parity-harness-<seam>.md` in a new session). End by suggesting, in one sentence,-] {+it — and then+} a new session [-and-] {+with+} that `/run` [-there-] (model assignments load at session start) — no /handoff needed. No rebuild step starts before that plan ships with the harness PASS against legacy.

claude/.claude/skills/backlog/SKILL.md ↔ opencode/commands/backlog.md (identical)

claude/.claude/skills/brainstorm/SKILL.md ↔ opencode/commands/brainstorm.md (identical)

claude/.claude/skills/brief/SKILL.md ↔ opencode/skills/brief/SKILL.md (identical)

claude/.claude/skills/build/SKILL.md ↔ opencode/commands/build.md
  0. read the plan file (it is short). If the current branch is already plan/<slug> (a /spawn [-or `opencode --worktree` checkout,-] {+worktree,+} or the board names it), stay on it and continue from the step after the last PASS the board records (a plan amended with `Revised: <date> (from step k)` continues at step k — the steps before it are unchanged and already committed; a header still marked `draft` is not approved: stop and say so) — or, without a board, after the last step commit on the branch (`git log --format=%s%n%b main..HEAD` names plan and step); step 1 when there is neither. In a linked worktree on any other branch, stay on it too — the branch was cut when the worktree was made. Otherwise, on main: apply the branches-and-sync rule (synced), make sure the plan file is committed (`git status --porcelain <plan path>` prints nothing; if not, team-implementer commits it as `docs(plan): NNNN-<slug>` first), then `git switch -c plan/<slug>` yourself (slug from the plan filename). If that branch already exists locally or as origin/plan/<slug>: `git switch plan/<slug>` when its PR is not MERGED (`gh pr view plan/<slug> --json state -q .state`, or no remote) — an abandoned earlier run continues there; when it is MERGED, stop and ask the CEO to delete it first. One plan = one branch. Record the plan and branch in docs/STATUS.md (Shipped: —).
  When all steps are done, run full verification with team-verifier — the independent verdict on the whole plan. On PASS, record `Last verifier: PASS full @<hash>` (`git log -1 --format=%H`) in docs/STATUS.md so /ship can reuse it. On FAIL, add one to the board's `verifier FAIL` count and pass the report verbatim to team-implementer together with the plan's step list (it locates the step by commit, fixes and commits); team-verifier confirms with full verification (max 3 rounds; past that, stop and report to the CEO). [-If the CEO invoked /build directly,-] {+Then+} suggest `/review` to the CEO in a sentence [-(inside-] {+— except in+} a [-/run chain,-] {+/spawn worktree session (the directory is not the first line of `git worktree list`): there, end with "close this session and+} run [-continues).-] {+`/integrate` in the main session" — the branch is reviewed, merged and shipped from there, never from the worktree.+}

claude/.claude/skills/charter/SKILL.md ↔ opencode/skills/charter/SKILL.md (identical)

claude/.claude/skills/claude-md/SKILL.md ↔ opencode/skills/agents-md/SKILL.md
  AGENTS.md is injected into every [-session and every subagent.-] {+session.+} It must be short and true.
  - (filled by /hire via set-profile.mjs: budget tier / default review lenses 2|4 / parallelism [-none|session|parallel-] {+none|session+} / plan-size target (logic steps, default 4) / per-step verifier on|off / step-size target / hire date)

claude/.claude/skills/handoff/SKILL.md ↔ opencode/commands/handoff.md (identical)

claude/.claude/skills/hire/SKILL.md ↔ opencode/commands/hire.md
  Hire the team (assign a model [-and effort-] per role). Note: $ARGUMENTS
  1. Candidates: [-the default candidates are the aliases opus / sonnet / haiku (they resolve-] {+run `opencode models`+} to [-the plan's latest models). Include other model IDs (API, Bedrock, Vertex) only if-] {+list available models. For models you know by name, summarize strengths (reasoning, code, speed, cost, context). For models you do not know (e.g. in-house), do not guess — ask+} the CEO [-names them. Plan-] {+which is strongest, which is fastest, context length+} and [-usage caps cannot be read from the CLI — ask, do not guess.-] {+cost constraints.+}
  2. Conditions (one question-tool call): budget tier (unlimited [-= API/in-house-] / high [-= Max 20x-] / medium [-= Max 5x-] / low [-= Pro),-] {+— sense of the usage cap),+} project character (docs/BRIEF.md via team-planner's report or the brainstorm session, docs/CHARTER.md and ASSESSMENT.md — a legacy rebuild weights critic and reviewer; a prototype weights speed), whether parallel development is planned, and any role to make deliberately strong or cheap.
  3. Proposal: a role × model [-× effort-] table with a one-sentence reason per row and a note of where tokens concentrate. Principle: [-strong-] {+the strongest+} model [-and high effort-] where judgment happens (planner, critic, reviewer); [-verifier on haiku·low;-] {+the fastest model for verifier;+} implementer by budget. Also propose an operating profile: default review lenses (2 or 4), parallelism (none / [-session-level / /spawn),-] {+session-level),+} plan-size target (default 4 logic steps — the critic reviews plans over it; bigger work is split by tryable outcome), per-step verifier (off by default — the implementer's red → green report carries each step and team-verifier gives the full verdict at the end of /build; on when the project wants an independent quick run per step; risk:high and legacy plans get it regardless), step-size target (default 300 changed lines of logic, tests excluded). On the low tier, drop parallelism and 4 lenses from the defaults.
  - [-Models and effort:-] {+Models:+} `node .opencode/scripts/apply-models.mjs --flavor opencode --dest .opencode/agents --budget inherit --set [-team-lead=<model>:<effort> --set team-planner=...-] {+team-lead=<id>+} --set [-team-critic=...-] {+team-planner=<id>+} --set [-team-implementer=...-] {+team-critic=<id>+} --set [-team-builder=...-] {+team-implementer=<id>+} --set [-team-reviewer=...-] {+team-reviewer=<id>+} --set [-team-verifier=...`. The session (lead) model is written to settings.json from the team-lead value.-] {+team-verifier=<id>`.+} The script changes only [-model/effort-] {+`model:`+} lines. Never edit agent files directly.
  - Operating profile: `node .opencode/scripts/set-profile.mjs "budget tier: <tier>" "default review lenses: <2|4>" "parallelism: [-<none|session|parallel>"-] {+<none|session>"+} "plan-size target: <n>" "per-step verifier: <on|off>" "step-size target: <n>" "hired: <date> — <one-line reason>"`. The script rewrites only the "## Operating profile" [-section;-] {+section of AGENTS.md;+} no planner call.
  - Commit: have team-implementer commit the files the scripts changed inside the repository (agent files under .opencode/, [-.opencode/settings.json,-] AGENTS.md), by path, as `chore(hire): <tier> — models and operating profile` on the current branch.
  5. Show the result in /roster form and note that model assignments take effect from the next session. {+If docs/STATUS.md lists `/hire` as its first next action, remove that line yourself so `/run …` comes first.+}

claude/.claude/skills/hotfix/SKILL.md ↔ opencode/commands/hotfix.md (identical)

claude/.claude/skills/integrate/SKILL.md ↔ opencode/commands/integrate.md
  Integrate branches: $ARGUMENTS [-(if-] {+(opencode has no way to mark a spawned branch "done" from this session — if+} empty, [-the completed entries of-] {+list+} the "Parallel in progress" [-list in docs/STATUS.md, in order)-] {+section of docs/STATUS.md and ask the CEO which branches are ready)+}
  1. Review `git diff <current>...<branch>` with team-reviewer across 4 lenses. If there are blockers/majors, {+merge anyway in step 2 and+} have team-implementer fix [-and commit inside that branch's worktree (`cd <worktree path> && ...`),-] {+on the integration branch in this checkout (worktrees outside the project are denied to it),+} then re-review only the fix diff with the lenses that objected (max 3 rounds, as in /review); past that, [-leave the branch out of-] {+undo+} the [-integration-] {+merge with step 3's revert+} and report it to the CEO.

claude/.claude/skills/kickoff/SKILL.md ↔ opencode/commands/kickoff.md
  5. Have team-implementer commit the files this command wrote, by path — docs/CHARTER.md, docs/adr/0001-stack.md, docs/BACKLOG.md, docs/plans/0001-walking-skeleton.md, docs/BRIEF.md and docs/DECISIONS.md if changed, AGENTS.md, docs/README.md, .gitignore whenever it is untracked or its `docs/STATUS.md` line is uncommitted (add the line when the installer did not), and .opencode/ and opencode.json if not yet tracked — on main, as `docs(kickoff): charter, stack ADR, backlog, plan 0001`. [-Then run `hire` via the Skill tool (the budget questions go to the CEO; applied and committed after approval) — the project's character is in the brief.-] write docs/STATUS.md (Now: none · Next actions: 1. {+`/hire` in this session 2.+} `/run docs/plans/0001-walking-skeleton.md` in a new session). End by [-suggesting,-] {+suggesting `/hire` to the CEO+} in [-one sentence,-] a {+sentence — the project's character is in the brief, so assign models per role there — and then a+} new session [-and-] {+with+} that `/run` [-there-] (model assignments load at session start) — no /handoff needed. Do not start it yourself.

claude/.claude/skills/lang/SKILL.md ↔ opencode/commands/lang.md (identical)

claude/.claude/skills/legacy-assessment/SKILL.md ↔ opencode/skills/legacy-assessment/SKILL.md (identical)

claude/.claude/skills/parallel/SKILL.md ↔ opencode/commands/spawn.md
  description: [-Run several approved, independent plans concurrently-] {+Parallel development+} — [-one team-builder-] {+create a worktree and branch+} per {+approved+} plan [-in its own isolated worktree.-] {+and print the command to open a new session there.+} Integrate [-afterwards-] with /integrate
  [-Implement-] {+Prepare+} plans {+to run+} in [-parallel:-] {+parallel in separate worktrees:+} $ARGUMENTS {+(plan file paths, space-separated)+}
  0. Apply the branches-and-sync rule (on main, synced); if not on main, or in a linked worktree, stop and ask the CEO [-(builders-] {+(worktrees must+} branch from [-the session HEAD, so starting on-] main [-keeps-] {+so+} /integrate {+stays+} simple). [-Preconditions:-] {+Precondition:+} each plan is approved and committed on main (`git status --porcelain <path>` prints nothing — `??` means untracked, ` M`/`A ` uncommitted; an uncommitted plan is committed first by team-implementer as `docs(plan): NNNN-<slug>`, because worktrees are cut from HEAD and would not see [-it), and their "files touched" sets do not overlap. If a pair overlaps, run those two sequentially (/build) or ask the CEO.-] {+it).+}
  [-1. Rewrite the board's Now block yourself first — `Plan: parallel — <n> plans · Branch: main · Shipped: —`, `Step: none`, Next actions: 1. `/integrate` — so a resumed session never re-runs a plan a builder is building. Then launch one team-builder subagent per plan, all in the same turn. Tell-] {+For+} each [-its plan file path and "implement this whole plan".-] {+plan:+}
  [-2. Start no new work while waiting. As each completion report arrives, record plan path · branch · worktree path · result under-] {+1. Check that+} the [-"Parallel in progress" list-] {+plan's "files touched" set does not overlap with other plans+} in [-docs/STATUS.md yourself (the entries stay until /ship clears them).-] {+progress. If it does, do not create the worktree; report to the CEO.+}
  [-3. When all-] {+2. `git worktree add ../<repo>-<slug> -b plan/<slug>` (repo = current directory name). If untracked files such as .env+} are [-done, suggest `/integrate` to-] {+needed, tell+} the CEO [-in a sentence. Leave failed builders' branches out of the integration list and report them-] {+which+} to [-the CEO.-] {+copy.+}
  {+3. Record plan path · slug · branch · path under the "Parallel in progress" list in docs/STATUS.md, and set the Now block to `Plan: parallel — <n> plans · Branch: main · Shipped: —`, `Step: none`, Next actions: 1. `/integrate` (after the worktree sessions finish) — so a resumed session never re-runs a plan a worktree session is building.+}
  {+4. Give the CEO the command to run in a new terminal: `cd ../<repo>-<slug> && opencode`, then `/build <plan path>` in that session (it stays on plan/<slug>; the worktree has no board).+}
  {+A session inside a worktree is independent of this one. Finished branches are integrated here with /integrate.+}
  {+Precondition: .opencode/ and opencode.json must be committed so worktree sessions also have the team (or install with --global).+}

claude/.claude/skills/parity-harness/SKILL.md ↔ opencode/skills/parity-harness/SKILL.md (identical)

claude/.claude/skills/plan/SKILL.md ↔ opencode/commands/plan.md
  0. Intake — one question-tool call before delegating [-anything.-] {+anything (split only if the tool limits questions per call).+} Skip it when the request already carries a verifiable done-when:
  0b. Revision — the five moves of /brainstorm's revision mode, for an approved spec (`Status: approved` or `in progress`) or a plan (approved, or running on plan/<slug> — stay on that branch, never switch). (1) Brainstorm mode: discuss the change — the options, what it touches, its cost; for a plan, the board's Step line and `git log --format=%s main..HEAD` tell you which steps are already committed. (2) When the CEO says "정리해줘" / "write it up": one team-planner call with the discussion verbatim (for a spec, load the `spec` skill first — its Revision section). A spec: only the differences (Done-when, Not-this-time, Undecided), `Status: … (rev n draft)`, a Revision-history line, the BACKLOG item's completion criteria when it has one, and an impact report — plans in flight (the board's Plan line and step), shipped plans (docs/METRICS.md lines naming the spec), conflicts with docs/CHARTER.md or the brief's non-goals. A plan: only the steps after the last PASS the board records (all steps when none ran), header `Revised: <date> (from step k, draft)`, sections 1–2 changed only when the source spec or brief changed (the Source line names the spec's rev), the step-size rules as usual. Update the board (`Spec: … rev n draft` or `Plan: … rev n draft`). (3) team-critic reviews only the changed parts — a spec: does it invalidate shipped work, can the plan in flight absorb it in its remaining steps, does it widen past the brief's non-goals or the charter; a plan: are only steps after the last PASS changed, does each changed step keep a verification command and the size rules, is it consistent with the spec's current rev. Findings only the CEO can settle go to the CEO in plain chat as the correction round (a spec: usually all of them); the rest to the planner per the critic loop. (4) One [-the question tool:-] {+question-tool call:+} approve / correct (the CEO reads the file and corrects in plain chat; the planner revises; ask again — max 2) / stop here (nothing committed; the file keeps its draft rev). (5) On approve: the planner sets `Status: approved <date> (rev n)` (a spec in progress keeps `in progress` with the rev noted; a plan loses the `, draft` marker in its Revised header) and, when shipped plans are affected, records one BACKLOG item `spec NNNN rev n — <delta>` (recording only); team-implementer commits by path — a spec: `docs(spec): NNNN-<slug> rev n` on the current branch; a plan: `docs(plan): NNNN-<slug> rev n` on its plan/<slug> branch, or on main when it has no branch yet. Update the board (the rev on the Plan or Spec line; Step unchanged) and end with one sentence — a spec with a plan in flight: "run `/plan <plan path> 수정: spec rev n 반영`"; a plan: "run `/run <plan path>` — it continues at step k+1 on the same branch". Never start /run yourself.

claude/.claude/skills/policy/SKILL.md ↔ opencode/commands/policy.md (identical)

claude/.claude/skills/recruit/SKILL.md ↔ opencode/commands/recruit.md
  2. One proposal, one question-tool call: role name `team-<slug>`, one-line description, when you will invoke it, permission preset (researcher: read-only + [-web-] {+explore+} · author: docs/ only · engineer: edit + bash + commits · judge: read-only, report ends with a verdict line), and the [-model with effort.-] {+model.+} Take [-them-] {+the model+} from the current roster without asking a budget question again: the sibling role of the same kind (judge → team-reviewer, engineer → team-implementer, author → team-planner, researcher → team-verifier) and name it explicitly. Offer "skill only" as the alternative and give your recommendation with the reason.
  4. Run `node .opencode/scripts/new-agent.mjs --flavor opencode --dest .opencode/agents --name team-<slug> --preset <preset> --description "<one line>" --body docs/roles/team-<slug>.md --like team-<sibling>` (`--model [-<model>:<effort>`-] {+<id>`+} instead of `--like` when the CEO named [-one).-] {+a model).+} The script writes {+only+} the new agent [-file and adds the name to team-lead's Agent(...) tool list, nothing else.-] {+file.+} Never create or edit agent files yourself. Then have team-implementer commit the new agent file, docs/roles/team-<slug>.md and the DECISIONS line (`chore(recruit): team-<slug>`) on the current branch.

claude/.claude/skills/release/SKILL.md ↔ opencode/commands/release.md (identical)

claude/.claude/skills/resume/SKILL.md ↔ opencode/commands/resume.md (identical)

claude/.claude/skills/retro/SKILL.md ↔ opencode/commands/retro.md
  2. Propose a countermeasure per cause and say where it belongs: one line in AGENTS.md / a skill change / a guardrail hook (hard block) / a "## Risk paths" glob / the plan format / [-agent memory /-] a new role via `/recruit` (only for a new permission boundary or a separate judge).

claude/.claude/skills/review/SKILL.md ↔ opencode/commands/review.md (identical)

claude/.claude/skills/roster/SKILL.md ↔ opencode/commands/roster.md
  read the frontmatter of .opencode/agents/team-*.md and [-the `model` in .opencode/settings.json and-] present a table: agent / {+mode /+} model ("inherited" if absent) / [-effort /-] tools or permissions summary / one-line role.
  - Budget profile: `./install.sh opencode -Budget [-unlimited|high|medium|low`-] {+unlimited|high|medium|low -Strong <provider/model> -Fast <provider/model>`+} or, without the template, `node .opencode/scripts/apply-models.mjs --flavor opencode --dest .opencode/agents --budget [-<tier>`-] {+<tier> --strong <provider/model> --fast <provider/model>`+}
  - Per-agent: `--budget inherit --set [-team-reviewer=<model>:<effort>`-] {+team-reviewer=<model>`+} (repeatable; other agents stay as they are). Back to inheritance for everyone: `--budget inherit` with no `--set`
  - [-Session model only: opencode's `/model`. Interactive editing: `/agents`.-] {+Available models: `opencode models`.+} The session [-(lead)-] model is [-the `model` value in settings.json.-] {+switched with `/models`.+}

claude/.claude/skills/run/SKILL.md ↔ opencode/commands/run.md
  Stop only at three places: an escalation question, a fix loop exceeding 3 rounds, and merge approval (when the policy resolves to local/manual). Otherwise proceed without asking the [-CEO.-] {+CEO, and report once at the end in the final report format.+}
  [-1. Run `build` via the Skill tool (argument: $ARGUMENTS). Do not continue until it finishes.-]
  [-2. Run `review` via the Skill tool (argument: base branch, normally main).-]
  [-3. Run `ship` via the Skill tool (argument: plan slug).-]
  {+1. Branch (= /build step 0): read the plan file.+} If {+the current branch is already plan/<slug> (a /spawn worktree, or the board names it), stay on it and continue from the step after the last PASS the board records (a plan amended with `Revised: <date> (from step k)` continues at step k — the steps before it are unchanged and already committed;+} a [-skill's stop condition triggers,-] {+header still marked `draft` is not approved:+} stop [-there-] and [-report what-] {+say so) — or, without a board, after the last step commit on the branch (`git log --format=%s%n%b main..HEAD` names plan and step); step 1 when there+} is [-blocked. When all three finish, report once in-] {+neither. In a linked worktree on any other branch, stay on it too —+} the [-final report format (what / how verified / remaining risks / decisions needed).-] {+branch was cut when the worktree was made. Otherwise, on main: apply the branches-and-sync rule (synced), make sure the plan file is committed (`git status --porcelain <plan path>` prints nothing; if not, team-implementer commits it as `docs(plan): NNNN-<slug>` first), then `git switch -c plan/<slug>` yourself (slug from the plan filename). If that branch already exists locally or as origin/plan/<slug>: `git switch plan/<slug>` when its PR is not MERGED (`gh pr view plan/<slug> --json state -q .state`, or no remote) — an abandoned earlier run continues there; when it is MERGED, stop and ask the CEO to delete it first. One plan = one branch. Record the plan and branch in docs/STATUS.md (Shipped: —).+}
  {+2. Implement (= /build): per step, in order, assign it to team-implementer with plan path, step number and the step's lines (its `proves:` lines included), then check its report for format only — every proves line has a `Proven:` line with red → green (or the plan's `n/a`), every quick command at exit 0; an incomplete report goes back to the implementer (max 3). team-verifier runs per step only when the operating profile says `per-step verifier: on`, the plan is risk:high or it is legacy work (a rebuild step, a parity harness); on FAIL, bump the board's `verifier FAIL` count and pass the report verbatim for a fix (max 3). Record the step on the board (`proven (self-check)` or `PASS quick`) and continue. After the last step, full verification with team-verifier — the independent verdict — and record `PASS full @<hash>` in STATUS.+}
  {+3. Review (= /review): the test-file floor first — a logic commit whose step has proves lines other than `n/a` but touches no test file (`git show --name-only`) stops here: report the step to the CEO, no reviewer spawned. Then 2 lenses (correctness, security), or 4 for risk:high / over 600 logic lines / hot paths, in parallel, each given the diff range and the proves lines → collect blocker/major → implementer fixes → verifier full, `PASS full @<hash>` recorded (max 3) → re-review only the fix diff with the lenses that objected → all APPROVE.+}
  {+4. Ship (= /ship): reuse the recorded full PASS if HEAD is unchanged or only docs-only files changed since, else full verification → merge-policy resolution (local / manual / auto-low-risk) → PR description ("Proven" first: each proves line's test, red → green, and the command that runs them; a "Try it" section for user-facing plans) + risk label → a planner call only when a spec Status line, an ADR or DECISIONS must change → one implementer call (METRICS line with `tests +n`, CHANGELOG, user docs, docs/prs if local, commit incl. the planner's files, tidy, push / PR / auto-merge per verdict — never auto-merge a PR with a "Try it" section) → local merge after CEO approval (a PR with a "Try it" section asks: approve and merge / I will try it first — board: Waiting on CEO / problem found → `/plan bug:`, no merge), or `git switch main` after the PR is created (manual / auto-low-risk; in a linked worktree stay on the worktree branch; the plan branch keeps its PR) → update STATUS (Plan line kept, Shipped: PR link or merge hash · Step: none · next: try it when the PR has a "Try it" section and merge, then `/plan <next BACKLOG item>`) → tell the CEO in one line to merge before the next /plan — after walking the "Try it" steps when the PR has them.+}

claude/.claude/skills/ship/SKILL.md ↔ opencode/commands/ship.md (identical)

claude/.claude/skills/spec/SKILL.md ↔ opencode/skills/spec/SKILL.md (identical)

claude/.claude/agents/team-critic.md ↔ opencode/agents/team-critic.md
  description: Adversarial reviewer of plans and design docs (pre-mortem). Finds holes, hidden assumptions and simpler alternatives. Never edits. [-Use right after a plan is written.-]

claude/.claude/agents/team-implementer.md ↔ opencode/agents/team-implementer.md
  description: Implements one step of a plan. Small diff, tests first (red → green per proves line), runs verification itself, commits and reports. [-Use for every code change.-] {+Invoked by the lead via task.+}

claude/.claude/agents/team-lead.md ↔ opencode/agents/team-lead.md
  description: GARAGISTE lead. Takes the CEO's goals, delegates planning, implementation, review and verification to subagents, and judges the results. [-Runs as the main-session agent (settings.agent or `opencode --agent team-lead`).-] {+Never edits code.+}
  - You do not write code. [-Only team-implementer (code)-] {+Changes go through team-implementer; judgments through team-verifier+} and [-team-planner (docs) edit files.-] {+team-reviewer.+} The [-one-] {+only+} file you edit yourself is [-docs/STATUS.md — the guardrail hook blocks your edit/write on every other path.-] {+docs/STATUS.md.+} You never stage, commit or push: milestone [-commits (brief, plan, spec, kickoff/assess docs, hire output)-] and step commits alike are made by [-team-implementer, and the guardrail hook blocks `git add`/`git commit` for you;-] {+team-implementer (you have no git commit permission);+} the local merges of /ship and /integrate and the sync rebase below are the only history you write yourself.
  - You maintain the status board docs/STATUS.md [-yourself (edit; that file only; keep-] {+(keep+} it under 30 lines — it is injected into every session): update it after every step completion, verifier result, review verdict and CEO decision, and bump the Counts line as things happen — a verifier FAIL, a blocker/major finding, an escalation question, a spec or brief correction round (intake, spec rounds, brief slots and the merge gate are expected and not counted); /ship reads METRICS from that line, never from memory. Format:
  - <plan path> · {+<slug> ·+} <branch> · <worktree path> [-· <result> |-] {+[·+} merged [-@<hash>-] {+@<hash>]+}
  - Branches and sync: plan, spec, brief, kickoff and assess commits land on main (or master — the default branch); step and ship commits on plan/<slug> (a hotfix's on hotfix/<slug>); integration merges on integrate/<date> (/integrate) — main receives them only through /ship; backlog, retro, recruit, hire, brief-revision and spec-revision commits go on whatever branch is checked out (a plan amendment on its plan branch) (on a plan branch they ship with its PR — never switch branches for them). Before /plan and before a plan starts (/run, /build, /spawn, /hotfix), be on main (unrelated uncommitted files may stay — only the rebase below needs a clean tree): from a plan/*, hotfix/* or integrate/* branch whose work is shipped (STATUS `Shipped:` is a PR link or merge commit) `git switch main`; from an unshipped one that is not the plan about to continue, ask the CEO; on the branch of the plan about to continue, stay and skip the rest. This applies to the main checkout only: in a linked worktree (your directory is not the first line of `git worktree list`, the main checkout — a [-`opencode --worktree` session or a-] /spawn [-worktree)-] {+worktree session)+} skip it entirely and stay on the worktree's branch; its commits reach main through its own PR or /integrate. With a remote: `git fetch origin`; if origin/main does not exist yet, skip the sync and tell the CEO in one line to push main first; else `git merge --ff-only origin/main`, and if the branches diverged `git rebase origin/main` (local main carries only milestone commits); if the merge or the rebase refuses to start, or the rebase stops on a conflict (`git rebase --abort`), ask the CEO — never stash or reset. Then `git branch -d` each plan/*, hotfix/* or integrate/* branch whose PR is MERGED (`gh pr view <branch> --json state -q .state`) — together with the plan/* branches the board lists as merged into that integrate/* branch — never without that check — a pushed branch has an upstream and `-d` deletes it even when unmerged.
  - The session is working memory; the repo is long-term memory. docs/STATUS.md is local (the project installer git-ignores it; after a global install add it to .gitignore yourself) and never committed — milestone commits carry the durable state, the board carries the pointer. When [-the board-] {+docs/STATUS.md+} is {+among the+} injected [-at session start,-] {+files,+} begin with /resume; when only the charter is, begin with `/plan <backlog item>` or `/backlog` and never suggest /resume; when neither is, [-the hook names the first command-] {+start from docs/BRIEF.md+} — [-/brainstorm,-] {+/brainstorm when it is missing+} or {+unfinished,+} /kickoff | /assess [-once docs/BRIEF.md-] {+(per its Kind line) when it+} is approved. /handoff is for a session that stops mid-flight; a finished /ship or /plan already leaves the board and the commits in place.
  - The unit of parallelism is a plan (feature), not a step. Only plans with non-overlapping file sets [-run concurrently-] {+are sent to separate worktree sessions+} via [-/spawn (team-builder).-] {+/spawn.+} Integration (/integrate) is always serial, into an integration branch that /ship ships as one PR or one local merge. Steps of one plan are implemented sequentially via /build.
  - Call subagents [-in the foreground (never `background: true`).-] {+synchronously.+} Wait for results; do not make tool calls just to check status. Parallelism means several task calls in one turn.
  - Do not read long files yourself: delegate searches, multi-file questions and anything over ~100 lines to explore and take back summaries. A single file whose path you know and expect under ~100 lines — the current plan, a skill, a docs/prs entry — read it directly; an explore spawn costs more than the file. CHARTER and STATUS are [-auto-injected: never-] {+auto-injected; do not+} re-read [-CHARTER, and read docs/STATUS.md once before your first edit of a session-] {+them+} (write [-it-] {+docs/STATUS.md+} in the format above when it [-does-] {+is+} not [-exist yet).-] {+among the injected files).+}

claude/.claude/agents/team-planner.md ↔ opencode/agents/team-planner.md
  description: Investigates requirements and the codebase and writes plans (docs/plans), specs, design docs, ADRs, [-AGENTS.md and-] the [-backlog.-] {+backlog and AGENTS.md.+} Never edits code. [-Use whenever documents must be written or updated.-]
  - [-Check agent memory first (architecture, past decisions, recurring mistakes). When done, leave a short note of anything new you learned.-] {+Delegate investigation to explore in parallel and write down conclusions only.+}

claude/.claude/agents/team-reviewer.md ↔ opencode/agents/team-reviewer.md
  description: Code review of a diff. The lead assigns one lens (correctness|security|performance|maintainability) and you go deep on that lens only. Never edits. [-Use on every finished diff.-]
  You are a senior reviewer. If no lens is given, use correctness. Review only the specified diff [-range (use Bash only for git diff/show).-] {+range.+}
  [-Before reviewing, check agent memory for this repo's recurring defect patterns; afterwards record any new pattern you found.-]

claude/.claude/agents/team-verifier.md ↔ opencode/agents/team-verifier.md
  description: Runs the verification commands from AGENTS.md (tests, lint, typecheck, build) and reports PASS/FAIL only. Never fixes code. [-Use-] {+Used+} at the end of /build, after a review fix round and before shipping; per step only when the operating profile or the plan's risk says so.

claude/GUIDE.ko.md ↔ opencode/GUIDE.ko.md
  | 병렬 개발 | `/spawn <계획들>` → {+각 세션 `/build` →+} `/integrate` → `/ship` [-(또는 `opencode --worktree`)-] | 파일 집합이 겹치지 않는지 확인, 통합 순서 결정 |
  | 세션 재개 | `/resume` | 상태판이 있을 [-때만(세션 시작 안내가 알려 준다).-] {+때만(주입된 파일을 보면 안다).+} 상태판과 저장소가 어긋나면 저장소 편 들기 |
  - --global → ~/.config/opencode (모든 [-레포; 기본 에이전트는 강제하지 않아 `opencode --agent team-lead` 로 시작).-] {+레포).+} 전역 설치는 프로젝트의 .gitignore를 건드릴 수 없으니 `docs/STATUS.md`는 직접 추가한다(잊으면 첫 이정표 커밋이 추가한다).
  첫 실행: 레포에서 `opencode` → [-폴더 신뢰 확인에 동의(훅이 켜진다) → settings의 `agent` 설정으로 team-lead가 메인 에이전트다-] {+Tab 으로 `team-lead` 선택+} → 먼저 `/brainstorm`(기획서), 그다음 신규면 `/kickoff`, 레거시면 `/assess`; 이어하기면 `/resume`.
  확인할 것: [-SessionStart 직후 "GARAGISTE: no product brief yet…" 안내가 보이는지(훅 동작), `/kickoff` 등이 슬래시 메뉴에 보이고 `charter`·`spec` 같은 지식 스킬은-] {+planner가 docs/ 밖을 못 쓰고 docs/ 안은 쓰는지(edit 패턴은 루트 기준 상대 경로: `docs/*`, `**/docs/**`는+} 안 [-보이는지.-] {+됨), 훅이 `git push --force`를 막는지.+}
  `/kickoff` 또는 `/assess` 가 끝나면 lead 가 `/hire` 를 [-실행한다.-] {+제안한다.+} lead 가 사용 가능한 모델을 확인하고 당신에게 예산 티어(unlimited / high=Max 20x / medium=Max 5x / low=Pro), 프로젝트 성격, 병렬 계획을 묻고 역할 × 모델 표를 이유와 함께 내놓는다. 당신은 표를 승인하거나 한두 줄 바꾼다. 원칙: 판단하는 자리(planner·critic·reviewer)에 강한 모델, verifier 는 가장 빠른 모델, implementer 는 예산에 따라. 승인하면 스크립트가 model 줄만 바꾸고 AGENTS.md 에 "운영 프로필"이 남는다. 새 세션부터 반영. 설치할 때 `-Budget [-<티어>`를-] {+<티어> -Strong <id> -Fast <id>`를+} 주면 kickoff/assess 세션부터 verifier가 빠른 모델로 돌고, `/hire`가 그 위에서 다듬는다. 예산이 바뀌면 `/hire` 를 다시 하고, 현재 배정은 `/roster` 로 본다. 설치 스크립트의 `--budget` 은 판단 없이 기계적으로 배분하는 비대화형 경로다.
  - 팀, `/kickoff`: 기획서에서 헌장 도출(질문 없음) → 스택 ADR(critic 리뷰) → 확인 질문 한 번(스택, 헌장 세 줄, 기획서의 미결 항목) → 골격(스모크 테스트 하나가 통과해 하네스가 생긴다) → AGENTS.md(Risk paths 씨앗 포함)·ADR 확정·기획서의 채택 목록에서 백로그·walking skeleton 계획(핵심 흐름의 가장 얇은 조각)을 한 번에 → 전체 검증 → 문서 커밋 → [-`/hire`(모델-] {+lead가 `/hire`를 제안한다(모델+} 배정, [-커밋).-] {+커밋; 이 세션에서 실행).+} 세션은 상태판을 쓴 채 끝난다. 첫 `/run`은 새 세션에서 한다(모델 배정은 세션 시작 때 로드된다) — 바로 시작하면 되고 `/handoff`는 필요 없다.
  - 팀, `/brainstorm`: 짧은 리빌드 기획서(Kind: 레거시) → 빈 슬롯 → 사전 부검 → 승인 → 커밋. 팀, `/assess`: 인벤토리(docs/ASSESSMENT.md; 기획서의 목표 상태와 충돌하는 "버그로 보이는 동작"은 표시된다) → 미지수 보고, 보존/수정 방침을 한 번에 → 전략과 첫 seam(ADR, docs/REBUILD_PLAN.md, AGENTS.md)과 기획서·평가에서 도출한 헌장(목표·비목표·성공 지표는 전략과 함께 당신이 확인한다) → 첫 seam의 parity harness 계획 → 문서 커밋 → [-`/hire`(리빌드는-] {+lead가 `/hire`를 제안한다(리빌드는+} critic·reviewer 비중을 높인다; [-커밋).-] {+커밋; 이 세션에서 실행).+} 세션은 상태판을 쓴 채 끝난다. 하네스는 새 세션에서 계획 0001로 `/run`을 통해 만든다(모델 배정은 세션 시작 때 로드된다) — 바로 시작하면 되고 `/handoff`는 필요 없다. 이후 리빌드 단계마다 그 단계의 seam까지 먼저 확장한다. 인벤토리를 보고 목표가 달라졌으면 `/brainstorm`으로 기획서를 고친다.
  - [-방식 1 (세션 병렬, 가장 견고): 터미널마다 `opencode --worktree <기능명>` → 각-] {+명령: 메인+} 세션에서 [-`/plan`-] {+`/spawn <계획 파일들>`+} → [-`/run`.-] {+팀이 계획마다 `../<레포>-<slug>` worktree와 `plan/<slug>` 브랜치를 만들고 새 터미널에서 실행할 명령을 알려준다 →+} 당신이 {+터미널을 열어 `cd ../<레포>-<slug> && opencode` → 그 세션에서 바로 `/build <계획>`(worktree 세션은 plan/<slug>에 머물고 상태판이 없다) → worktree 세션을 닫고, 끝난 브랜치는 메인 세션에서 `/integrate`로 main에서 딴 통합 브랜치(`integrate/<날짜>`)에 하나씩 머지 → `/ship`이 그 브랜치를+} PR [-순서대로 머지하고, 뒤 브랜치 세션에는 "main에 rebase 후 재검증"을 시킨다. `opencode --worktree` 세션에서는 `/plan`과 `/run`이 worktree의 브랜치에 머문다.-] {+하나(원격이 없으면 로컬 머지 하나)로 출하.+}
  - [-방식 2 (세션 내 병렬): 메인 세션에서 `/spawn <계획 파일들>` → team-builder가 계획마다 격리 worktree에서 통째로 구현·자체 검증·커밋 → `/integrate`로 main에서 딴 통합 브랜치(`integrate/<날짜>`)에 하나씩 리뷰·머지·검증 → `/ship`이-] {+전제: `.opencode/`와 `opencode.json`이 커밋되어 있어야 worktree 세션에도 팀이 있다(아니면 `--global` 설치). `.env` 같은 미추적 파일은 worktree에 직접 복사한다. 계획은 승인 시 커밋되고, worktree가 보는 것도+} 그 [-브랜치를 PR 하나(원격이 없으면 로컬 머지 하나)로 출하. `docs/STATUS.md`의 "통합 대기" 목록으로 진행을 본다.-] {+커밋이다.+}
  - [-방식 3 (agent teams, 실험적): `settings.json`의 `env`에 `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1`. 모듈이 진짜 독립적이고 팀원끼리 조율이 필요할 때만.-] {+opencode의 subagent는 worktree 격리 기능이 없어 "세션 내 병렬"은 지원하지 않는다. 병렬 = 세션 병렬이다.+}
  [-- 전제: `.opencode/`가 커밋되어 있거나 `.worktreeinclude`에 올라 있어야 worktree 세션에도 팀이 있다. `.env`도 `.worktreeinclude`에 적는다. 계획은 승인 시 커밋되고, worktree가 보는 것도 그 커밋이다.-]
  - 명령: 새 세션 → [-세션 시작 안내에-] {+주입된 파일에+} 상태판이 [-보이면-] {+있으면+} `/resume [메모]`. 헌장만 있으면 `/plan`으로 바로 시작한다(진행 중인 것이 없다). 짧게 끊긴 경우만 `opencode -c` 로 직전 세션을 이어간다. 길게 이어진 세션은 요약 위에 요약이 쌓여 품질이 떨어지니 기본은 새 세션이다.
  [-- planner와 reviewer는 `memory: project`로 `.opencode/agent-memory/<name>/`에 세션을 넘어 지식을 쌓는다(아키텍처, 반복 결함). auto memory가 꺼져 있으면 동작하지 않는다.-]
  - 팀: docs/METRICS.md 추세(재작업·질문·소요)를 먼저 보고 → 재작업·검증 실패·리뷰 blocker·CEO 개입이 난 지점을 3개 이내로 특정, 근본 원인 → 원인마다 재발 방지책과 그것이 박힐 자리(규칙 한 줄 / 스킬 / 훅(하드 차단) / 계획 [-형식 / 에이전트 memory)-] {+형식)+} 제안 → 승인된 것만 planner가 반영. 접수·사양서 라운드는 개입으로 세지 않는다. 교정 상한에 걸린 사양서나 기획서, 두 번 이상 수정된 계획은 회고 대상이다.
  - 언제: `/kickoff`·`/assess` 직후(lead가 [-실행한다),-] {+제안한다),+} 예산 티어가 바뀔 때, 팀이 너무 느리거나 비싸다고 느낄 때.
  - 팀: [-별칭 opus/sonnet/haiku(와 당신이 알려준 API 모델 ID)를 후보로 놓고,-] {+`opencode models`로 목록을 얻고,+} 예산 티어·프로젝트 성격·병렬 계획·강조할 역할을 한 번에 묻는다 → 역할 × 모델 표를 이유와 함께 제안 → 운영 프로필(기본 리뷰 렌즈 2/4, 병렬 허용, 계획 크기 목표, 단계별 verifier — 기본은 끔, 단계 크기 목표)도 제안 → 승인 후 스크립트 하나가 model 줄만 바꾸고 다른 하나가 AGENTS.md에 "운영 프로필" 절을 쓴다.
  - 자동 주입 문서가 비대해짐: CHARTER 60줄·STATUS 30줄 상한. STATUS는 lead가 상한 안에 유지하고, CHARTER는 넘치면 planner가 분리한다. [-훅은 120줄에서 자른다.-] {+opencode 에는 (Claude Code 의 session-start 훅과 달리) 강제 상한이 없어서, 이 선을 넘긴 파일은 그대로 매 세션 전체가 주입된다.+}

claude/GUIDE.md ↔ opencode/GUIDE.md
  | Parallel development | `/spawn <plans>` → {+`/build` in each session →+} `/integrate` → `/ship` [-(or `opencode --worktree`)-] | Check that file sets do not overlap; decide the integration order |
  | Resuming a session | `/resume` | Only when a board exists (the [-session start says so);-] {+injected files show whether one does);+} if board and repo disagree, side with the repo |
  - `-Global` → ~/.config/opencode (every [-repo; no default agent is forced, start with `opencode --agent team-lead`).-] {+repo).+} A global install cannot touch a project's .gitignore: add `docs/STATUS.md` there yourself (the first milestone commit does it when you forget).
  First run: run `opencode` in the repo → [-accept the folder-trust prompt (this enables hooks) → `settings.agent` makes team-lead the main agent-] {+Tab to select `team-lead`+} → `/brainstorm` first (the product brief), then `/kickoff` for a new project or `/assess` for legacy; `/resume` to continue.
  Check: [-right after SessionStart you see "GARAGISTE: no product brief yet…" (hook works),-] {+the planner can write inside docs/ but not outside (edit patterns are repo-relative: `docs/*`, never `**/docs/**`),+} and [-`/kickoff` etc. appear in-] the [-slash menu while knowledge skills such as `charter` or `spec` do not.-] {+plugin blocks `git push --force`.+}
  When `/kickoff` or `/assess` finishes, the lead [-runs-] {+suggests+} `/hire`. It confirms available models [-(aliases opus/sonnet/haiku),-] {+(`opencode models`),+} asks you for a budget tier (unlimited / high = Max 20x / medium = Max 5x / low = Pro), the project's character and parallel plans, and proposes a role × model table with reasons. You approve it or change a line or two. Principle: the strongest model where judgment happens (planner, critic, reviewer), the fastest model for the verifier, the implementer by budget. On approval a script changes only the `model:` lines and an "Operating profile" is left in AGENTS.md. Takes effect from the next session. Pass `-Budget [-<tier>`-] {+<tier> -Strong <id> -Fast <id>`+} at install time so the kickoff/assess session itself already runs the verifier on the fast model; `/hire` then refines it. Re-run `/hire` when the budget changes; `/roster` shows the current assignment. The installer's `-Budget` profiles distribute mechanically without judgment, for non-interactive use.
  - Team, `/kickoff`: charter derived from the brief (no questions) → stack ADR (critic-reviewed) → one confirmation call (the stack, the three charter lines, the brief's undecided items) → skeleton (one smoke test passes, so a harness exists) → AGENTS.md (Risk paths seeded), ADR decision, backlog from the brief's decided lines and a walking-skeleton plan (the thinnest slice of the brief's core flow) in one pass → full verification → docs commit → {+the lead suggests+} `/hire` (model assignment, [-committed).-] {+committed; run it in this session).+} The session ends with the board written; the first `/run` happens in a new session (model assignments load at session start) — start it directly, no `/handoff`.
  - Team, `/brainstorm`: a short rebuild brief (Kind: 레거시) → open slots → pre-mortem → approval → commit. Team, `/assess`: inventory (docs/ASSESSMENT.md; "looks like a bug" items that conflict with the brief's target state are marked) → unknowns reported, preserve/fix policy in one call → strategy and first seam (ADR, docs/REBUILD_PLAN.md, AGENTS.md) and the charter derived from the brief and the assessment (you confirm goal, non-goals and success metric together with the strategy) → a plan for the first seam's parity harness → docs commit → {+the lead suggests+} `/hire` (a rebuild weights critic and reviewer; [-committed).-] {+committed; run it in this session).+} The session ends with the board written; the harness is built through `/run` as plan 0001 in a new session (model assignments load at session start) — start it directly, no `/handoff`. Every later rebuild step extends it to its seam first. If the inventory changes the target, revise the brief with `/brainstorm`.
  - [-Option 1 (session-level, most robust): one terminal-] {+Command: in the main session, `/spawn <plan files>` → the team creates a `../<repo>-<slug>` worktree and `plan/<slug>` branch+} per [-feature with `opencode --worktree <feature>`-] {+plan and prints the command for a new terminal+} → [-each session runs `/plan`-] {+you open a terminal: `cd ../<repo>-<slug> && opencode`+} → [-`/run`. You merge PRs-] {+`/build <plan>` directly+} in [-order-] {+that session (the worktree session stays on plan/<slug>+} and [-ask later-] {+has no board) → close the worktree session, then finished+} branches [-to "rebase on-] {+are integrated in the+} main [-and re-verify". In a `opencode --worktree`-] session [-`/plan` and `/run` stay on the worktree's branch.-] {+with `/integrate` into an integration branch cut from main (`integrate/<date>`) → `/ship` ships that branch as one PR (or one local merge).+}
  - [-Option 2 (in-session): in the main session, `/spawn <plan files>` → one team-builder per plan implements, self-verifies-] {+Precondition: `.opencode/`+} and [-commits in an isolated-] {+`opencode.json` must be committed so+} worktree [-→ `/integrate` reviews, merges and verifies one branch at a time into an integration branch cut from main (`integrate/<date>`) → `/ship` ships that branch as one PR-] {+sessions also have the team+} (or [-one local merge). Track progress in-] {+install with `-Global`). Copy untracked files such as `.env` into+} the [-"Parallel in progress" list in `docs/STATUS.md`.-] {+worktree yourself. Plans are committed at approval, which is what worktrees see.+}
  - [-Option 3 (agent teams, experimental): `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1` in `settings.json` → `env`. Only when modules are truly independent and teammates need to coordinate.-] {+opencode subagents have no worktree isolation, so "in-session parallelism" is not supported. Parallel means session-level.+}
  [-- Precondition: `.opencode/` must be committed or listed in `.worktreeinclude` so worktree sessions also have the team. Add `.env` to `.worktreeinclude` as well. Plans are committed at approval, which is what worktrees see.-]
  - Command: new session → `/resume [note]` when the [-session start shows-] {+injected files include+} a board; with only a charter, start with `/plan` (nothing is in progress). Use `opencode -c` only for a short interruption. Long sessions stack summaries on summaries and degrade; the default is a fresh session.
  [-- The planner and reviewer accumulate knowledge across sessions in `.opencode/agent-memory/<name>/` via `memory: project` (architecture, recurring defects). Requires auto memory to be enabled.-]
  - Team: reads the docs/METRICS.md trend first (rework, questions, duration) → pinpoints at most three places where rework, verification failures, review blockers or CEO intervention occurred, with root causes → proposes a countermeasure per cause and where it belongs (one rule line / a skill / a hook (hard block) / the plan [-format / agent memory)-] {+format)+} → the planner applies only what you approve. Intake and spec rounds are not counted as intervention; a spec or brief that hit the correction cap, or a plan amended more than once, is a subject.
  - When: right after `/kickoff` or `/assess` (the lead [-runs-] {+suggests+} it), when the budget tier changes, when the team feels too slow or too expensive.
  - Team: [-takes the aliases opus/sonnet/haiku (and any API model IDs you name) as candidates;-] {+lists models with `opencode models`;+} asks budget tier, project character, parallel plans and roles to emphasize in one go → proposes a role × model table with reasons → also proposes the operating profile (default lenses 2/4, parallelism, plan-size target, per-step verifier — off by default, step-size target) → on approval, one script changes only model lines and another writes "## Operating profile" to AGENTS.md.
  - Auto-injected documents growing: CHARTER 60 lines, STATUS 30 lines. The lead keeps STATUS within the cap; the planner splits CHARTER when [-exceeded;-] {+exceeded — there is no hard cap in opencode (unlike+} the [-hook truncates at 120 lines.-] {+Claude Code flavor's session-start hook), so a file that grows past this stays uncapped and is injected in full every session.+}

claude/README.ko.md ↔ opencode/README.ko.md
  only in claude: /agents
  only in claude: /model
  only in claude: guardrails.mjs
  only in claude: team-builder

claude/README.md ↔ opencode/README.md
  only in claude: /agents
  only in claude: /command
  only in claude: /model
  only in claude: /skill
  only in claude: guardrails.mjs
  only in claude: team-builder

claude/docs/README.md ↔ opencode/docs/README.md
  CHARTER.md and STATUS.md are injected into every session (via [-the SessionStart hook).-] {+`instructions` in opencode.json).+} STATUS.md is local to the checkout — each worktree has its own — and is never committed (the project installer git-ignores it; after a global install add the line yourself); milestone commits (docs(brief), docs(plan), docs(kickoff), …) carry the durable state. BRIEF.md is never injected: the commands that need it name it.

claude/scripts/apply-models.mjs ↔ opencode/scripts/apply-models.mjs (identical)

claude/scripts/new-agent.mjs ↔ opencode/scripts/new-agent.mjs (identical)

claude/scripts/set-language.mjs ↔ opencode/scripts/set-language.mjs (identical)

claude/scripts/set-profile.mjs ↔ opencode/scripts/set-profile.mjs (identical)

README.md ↔ README.ko.md (identical)

claude/GUIDE.md ↔ claude/GUIDE.ko.md
  en "GARAGISTE User Guide — Claude Code flavor" | ko "GARAGISTE 사용 가이드 — Claude Code 판": para 2→3

claude/README.md ↔ claude/README.ko.md
  en "GARAGISTE — Claude Code flavor" | ko "garagiste/claude — GARAGISTE — Claude Code 판": para 2→5
  en "Install" | ko "설치": para 2→5
  en "Session lifecycle" | ko "세션 수명주기": para 1→3

opencode/GUIDE.md ↔ opencode/GUIDE.ko.md
  en "GARAGISTE User Guide — opencode flavor" | ko "GARAGISTE 사용 가이드 — opencode 판": para 2→3

opencode/README.md ↔ opencode/README.ko.md
  en "GARAGISTE — opencode flavor" | ko "GARAGISTE — opencode 판": para 2→5
  en "Install" | ko "설치": para 4→7
