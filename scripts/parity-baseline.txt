# GARAGISTE parity baseline — the accepted divergence between the claude and opencode flavors, after normalization
# (scripts/parity-check.mjs rewrites the claude side into opencode vocabulary first). Written by
# `node scripts/parity-check.mjs --update`; do not edit by hand. A `git diff` of this file shows which divergent
# lines a change touched. Content lines are word-diffs, claude [-removed-] and opencode {+added+}; structure lines
# compare a doc with its .ko.md translation per section; coverage lists files with no counterpart.

coverage
  claude-only: claude/.claude/agents/team-builder.md

claude/.claude/skills/assess/SKILL.md ↔ opencode/commands/assess.md
  5. Commit the files this command wrote yourself, by path — docs/ASSESSMENT.md, docs/CHARTER.md, docs/adr/0001-rebuild-strategy.md, docs/REBUILD_PLAN.md, docs/plans/0001-parity-harness-<seam>.md, docs/DECISIONS.md, docs/BRIEF.md if changed, AGENTS.md, docs/README.md, docs/STATUS.md, .gitignore if untracked, and .opencode/ and opencode.json if not yet tracked — on main (if the legacy checkout is on another branch, ask the CEO first), as `docs(assess): assessment, charter, rebuild ADR, rebuild plan, plan 0001`; the legacy tree's other uncommitted changes stay untouched. [-Run `hire` via the Skill tool with the budget tier from step 3 and the project's character from the brief (it applies without a second question and shows the roster) — a rebuild weights critic and reviewer heavily, so assign in that direction; the brief says how much rides on it.-] write docs/STATUS.md (`Mode: running` · Stage: live · Run: the AGENTS.md build/run line · Iteration 1 · Plans: docs/plans/0001-parity-harness-<seam>.md — · Now: none · Next actions: 1. {+`/hire` in this session 2.+} `deliver` — iteration 1, in a new session) and commit it by path (`docs(status): assess done`). End {+by suggesting `/hire` to the CEO in a sentence,+} with [-one sentence: start-] {+the budget tier from step 3 (it applies without+} a {+second question) — a rebuild weights critic and reviewer heavily, so assign in that direction; the brief says how much rides on it — and then a+} new session and [-say-] 계속 (model assignments load at session start; the board's Mode makes that session continue by itself). No rebuild step starts before that plan ships with the harness PASS against legacy.

claude/.claude/skills/backlog/SKILL.md ↔ opencode/commands/backlog.md (identical)

claude/.claude/skills/brainstorm/SKILL.md ↔ opencode/commands/brainstorm.md
  2. Brainstorm — brainstorm mode (team-lead principles): no agenda and no slot list. The CEO throws ideas; you propose, push back, sketch alternatives and name costs, at length when useful. Keep a whiteboard in your head: decided / later / rejected with the reason / open. Every ten or so exchanges — and always before a /handoff or when compaction looms — checkpoint it: one team-planner call with the CEO's messages verbatim and each of your proposals the CEO accepted, quoted, in order; the planner appends them to docs/BRIEF.md under "## Whiteboard" (`Status: whiteboard` in create mode; in revision mode the existing Status line stays and carries `(rev n draft)`) and reports the brief line; update docs/STATUS.md yourself (the [-`resume` skill's-] {+/resume command's+} Board format, [-loaded-] {+read+} once at the first board write; `Brief: docs/BRIEF.md · brainstorm (checkpoint k)`, or `revision (checkpoint k)` in revision mode; next action `/brainstorm`). The brainstorm ends when the CEO asks for the document ("기획서 만들어줘", "정리해줘", "write it up", or the like); a CEO who opens with a full description and asks for the document at once skips straight to step 3.
  - commit by path yourself — docs/BRIEF.md, docs/BACKLOG.md when seeded, docs/CHARTER.md when revised, docs/README.md, AGENTS.md (its Language section), docs/STATUS.md, .gitignore if untracked, and .opencode/ and opencode.json if not yet tracked — as `docs(brief): <title> — approved` on main (branches-and-sync rule; if the checkout is on another branch, ask the CEO first) or, for a revision, `docs(brief): revised — <summary>` on the current branch — then continue with that [-command (`kickoff`-] {+command's procedure (read .opencode/commands/kickoff.md+} or [-`assess`)-] {+assess.md)+} in the same turn, unless the brainstorm was long (a compaction happened): then say in one sentence to start a new session and say 계속 — the board's next action carries it.

claude/.claude/skills/brief/SKILL.md ↔ opencode/skills/brief/SKILL.md (identical)

claude/.claude/skills/build/SKILL.md ↔ opencode/commands/build.md
  0. read the plan file (it is short). If the current branch is already plan/<slug> (a /spawn [-or `opencode --worktree` checkout,-] {+worktree,+} or the board names it), stay on it and continue from the step after the last PASS the board records (a plan amended with `Revised: <date> (from step k)` continues at step k — the steps before it are unchanged and already committed; a header still marked `draft` is not approved: stop and say so) — or, without a board, after the last step commit on the branch (`git log --format=%s%n%b main..HEAD` names plan and step); step 1 when there is neither. In a linked worktree on any other branch, stay on it too — the branch was cut when the worktree was made. Otherwise, on main: apply the branches-and-sync rule (synced), make sure the plan file is committed (`git status --porcelain <plan path>` prints nothing; if not, commit it yourself as `docs(plan): NNNN-<slug>` first), then `git switch -c plan/<slug>` yourself (slug from the plan filename). If that branch already exists locally or as origin/plan/<slug>: `git switch plan/<slug>` when its PR is not MERGED (`gh pr view plan/<slug> --json state -q .state`, or no remote) — an abandoned earlier run continues there; when it is MERGED, stop and ask the CEO to delete it first. One plan = one branch. Record the plan and branch in docs/STATUS.md (Shipped: —; in a linked worktree skip the board — it is the main checkout's). Uncommitted changes beyond docs-only files on the branch are an interrupted step's work: name them to team-implementer as that step's starting point (it runs the step's tests first and continues from there) instead of starting the step over.
  When all steps are done, run full verification with team-verifier — the independent verdict on the whole plan. On PASS, record `Last verifier: PASS full @<hash>` (`git log -1 --format=%H`) in docs/STATUS.md so /ship can reuse it. On FAIL, add one to the board's `verifier FAIL` count and pass the report verbatim to team-implementer together with the plan's step list (it locates the step by commit, fixes and commits); team-verifier confirms with full verification (max 3 rounds; past that, stop and report to the CEO). When the Commands section of AGENTS.md still carries ` — unverified` markers (the first build after a kickoff), one team-planner call with the verifier's per-command results verbatim removes the marker from what passed and drops what failed (a command the guardrail blocked stays marked — tell the CEO its tool must be added to the verifier allow-list by a human); commit AGENTS.md yourself by path (`docs(agents-md): commands verified`) on the plan branch. [-If the CEO invoked /build directly,-] {+Then+} suggest `/review` to the CEO in a sentence [-(inside-] {+— except in+} a [-/run chain,-] {+/spawn worktree session (the directory is not the first line of `git worktree list`): there, end with "close this session and+} run [-continues).-] {+`/integrate` in the main session" — the branch is reviewed, merged and shipped from there, never from the worktree.+}

claude/.claude/skills/charter/SKILL.md ↔ opencode/skills/charter/SKILL.md (identical)

claude/.claude/skills/claude-md/SKILL.md ↔ opencode/skills/agents-md/SKILL.md
  AGENTS.md is injected into every [-session and every subagent.-] {+session.+} It must be short and true.
  - List only commands that were actually run and passed (confirmed by team-verifier). At kickoff and at assess each command line ends with ` — unverified` until team-verifier has run it: at assess the verifier runs them at once and the planner removes the marker before the rebuild plan is written; after a kickoff, the full verification at the end of plan 0001's build does (the planner drops the marker from what passed and the commands that failed, per the `build` [-skill).-] {+command).+} The guardrail strips the marker when it reads the list; the implementer and the verifier run the command without it.
  - (filled by /hire via set-profile.mjs: budget tier / default review lenses 2|4 / parallelism [-none|session|parallel-] {+none|session+} / plan-size target (logic steps, default 4) / per-step verifier on|off / step-size target / hire date)

claude/.claude/skills/deliver/SKILL.md ↔ opencode/commands/deliver.md
  The board's `Mode:` line is the contract with the CEO: `running` from the first step here to the pause at the end, `waiting on CEO` while a question is open, `paused — …` when the team is looking at the CEO and doing nothing else. Every step is re-entrant (the [-`resume` skill's-] {+/resume command's+} Continuity section): a session that opens with `Mode: running` re-enters here after `resume` and continues with the plan and step the board names — the CEO never has to repeat the go.
  a. [-`cat .opencode/session/compactions` — at 1 or more,-] {+a compaction summary in your context is+} the compaction cut (the [-`resume` skill's-] {+/resume command's+} Continuity [-section)-] {+section): it+} applies before a new plan starts.
  b. [-Run `plan` via-] {+Do+} the [-Skill tool (the item)-] {+/plan procedure (read .opencode/commands/plan.md) for the item+} — unless the item is already an approved plan path (docs/plans/0001-… after a kickoff or an assess): then skip to c. The critic's APPROVE is the approval. An escalation question that gets no answer in this turn ends the turn with `Mode: waiting on CEO` (the Waiting-on-CEO line carries the default); the CEO's next message resumes here.
  c. [-Run `run` via-] {+Do+} the [-Skill tool (the-] {+/run procedure (read .opencode/commands/run.md) for the+} plan [-path):-] {+path:+} build → review → ship. /ship merges when the lead may (lead-merge: risk:low, verifier PASS, every lens APPROVE, required checks green when there is a remote); a `risk:high` plan is merged by the lead as well while the board's Stage is `pre-launch`; at `live` it is the CEO's word, asked by /ship from the PR's Risk summary — with no answer in this turn, record it under Waiting on CEO, set `Mode: waiting on CEO` and end the turn, because the next plan may build on it.

claude/.claude/skills/design/SKILL.md ↔ opencode/skills/design/SKILL.md (identical)

claude/.claude/skills/handoff/SKILL.md ↔ opencode/commands/handoff.md (identical)

claude/.claude/skills/hire/SKILL.md ↔ opencode/commands/hire.md
  Hire the team (assign a model [-and effort-] per role). Note: $ARGUMENTS
  1. Candidates: [-the default candidates are the aliases opus / sonnet / haiku (they resolve-] {+run `opencode models`+} to [-the plan's latest models). Include other model IDs (API, Bedrock, Vertex) only if-] {+list available models. For models you know by name, summarize strengths (reasoning, code, speed, cost, context). For models you do not know (e.g. in-house), do not guess — ask+} the CEO [-names them. Plan-] {+which is strongest, which is fastest, context length+} and [-usage caps cannot be read from the CLI — ask, do not guess.-] {+cost constraints.+}
  2. Conditions — skipped when the caller passed them; otherwise one question-tool call: budget tier (unlimited [-= API/in-house-] / high [-= Max 20x-] / medium [-= Max 5x-] / low [-= Pro),-] {+— sense of the usage cap),+} project character (docs/BRIEF.md via team-planner's report or the brainstorm session, docs/CHARTER.md and ASSESSMENT.md — a legacy rebuild weights critic and reviewer; a prototype weights speed), whether parallel development is planned, and any role to make deliberately strong or cheap.
  3. Proposal: a role × model [-× effort-] table with a one-sentence reason per row and a note of where tokens concentrate. Principle: [-strong-] {+the strongest+} model [-and high effort-] where judgment happens (planner, critic, reviewer); [-verifier on haiku·low;-] {+the fastest model for verifier;+} implementer by budget. Also propose an operating profile: default review lenses (2 or 4), parallelism (none / [-session-level / /spawn),-] {+session-level),+} plan-size target (default 4 logic steps — the critic reviews plans over it; bigger work is split by tryable outcome), per-step verifier (off by default — the implementer's red → green report carries each step and team-verifier gives the full verdict at the end of /build; on when the project wants an independent quick run per step; risk:high and legacy plans get it regardless), step-size target (default 300 changed lines of logic, tests excluded). On the low tier, drop parallelism and 4 lenses from the defaults.
  - [-Models and effort:-] {+Models:+} `node .opencode/scripts/apply-models.mjs --flavor opencode --dest .opencode/agents --budget inherit --set [-team-lead=<model>:<effort> --set team-planner=...-] {+team-lead=<id>+} --set [-team-critic=...-] {+team-planner=<id>+} --set [-team-implementer=...-] {+team-critic=<id>+} --set [-team-builder=...-] {+team-implementer=<id>+} --set [-team-reviewer=...-] {+team-reviewer=<id>+} --set [-team-verifier=...`. The session (lead) model is written to settings.json from the team-lead value.-] {+team-verifier=<id>`.+} The script changes only [-model/effort-] {+`model:`+} lines. Never edit agent files directly.
  - Operating profile: `node .opencode/scripts/set-profile.mjs "budget tier: <tier>" "default review lenses: <2|4>" "parallelism: [-<none|session|parallel>"-] {+<none|session>"+} "plan-size target: <n>" "per-step verifier: <on|off>" "step-size target: <n>" "hired: <date> — <one-line reason>"`. The script rewrites only the "## Operating profile" [-section;-] {+section of AGENTS.md;+} no planner call.
  - Commit: commit the files the scripts changed inside the repository yourself (agent files under .opencode/, [-.opencode/settings.json,-] AGENTS.md), by path, as `chore(hire): <tier> — models and operating profile` on the current branch.
  5. Show the result in /roster form and note that model assignments take effect from the next session. {+If docs/STATUS.md lists `/hire` as its first next action, remove that line yourself so `/run …` comes first.+}

claude/.claude/skills/hotfix/SKILL.md ↔ opencode/commands/hotfix.md (identical)

claude/.claude/skills/instruction/SKILL.md ↔ opencode/skills/instruction/SKILL.md (identical)

claude/.claude/skills/integrate/SKILL.md ↔ opencode/commands/integrate.md
  Integrate branches: $ARGUMENTS [-(if-] {+(opencode has no way to mark a spawned branch "done" from this session — if+} empty, [-the completed entries of-] {+list+} the "Parallel in progress" [-list in docs/STATUS.md, in order)-] {+section of docs/STATUS.md and ask the CEO which branches are ready)+}
  1. Review `git diff <current>...<branch>` with team-reviewer across 4 lenses. If there are blockers/majors, {+merge anyway in step 2 and+} have team-implementer fix [-and commit inside that branch's worktree (`cd <worktree path> && ...`),-] {+on the integration branch in this checkout (worktrees outside the project are denied to it),+} then re-review only the fix diff with the lenses that objected (max 3 rounds, as in /review); past that, [-leave the branch out of-] {+undo+} the [-integration-] {+merge with step 3's revert+} and report it to the CEO.

claude/.claude/skills/kickoff/SKILL.md ↔ opencode/commands/kickoff.md
  4. Commit the files this command wrote yourself, by path — docs/CHARTER.md, docs/adr/0001-stack.md, docs/SPEC.md, docs/specs/, docs/BACKLOG.md, docs/plans/0001-walking-skeleton.md, docs/DECISIONS.md, docs/BRIEF.md if changed, AGENTS.md, docs/README.md, docs/STATUS.md, .gitignore if untracked, and .opencode/ and opencode.json if not yet tracked — on main, as `docs(kickoff): charter, stack ADR, spec, backlog, plan 0001`. [-Then run `hire` via the Skill tool with the budget tier from step 1 and the project's character from the brief (it applies without a second question and shows the roster; the CEO changes a line by saying so).-] write docs/STATUS.md (`Mode: running` · Stage: pre-launch · Run: not yet (the AGENTS.md build/run line once plan 0001 ships) · Iteration 1 · Plans: docs/plans/0001-walking-skeleton.md — · Now: none · Next actions: 1. {+`/hire` in this session 2.+} `deliver` — iteration 1, in a new session) and commit it by path (`docs(status): kickoff done`). End with one report — the spec sections (F1–F<n>, one line each, the pending ones marked), what was decided by default (a pointer to [-docs/DECISIONS.md), the roster-] {+docs/DECISIONS.md)+} — and [-one sentence: start-] a {+sentence suggesting `/hire` with the budget tier from step 1 (it applies without a second question and shows the roster), then a+} new session and [-say-] 계속 (model assignments load at session start; the board's Mode makes that session continue by itself). Do not start the iteration in this session.

claude/.claude/skills/lang/SKILL.md ↔ opencode/commands/lang.md (identical)

claude/.claude/skills/legacy-assessment/SKILL.md ↔ opencode/skills/legacy-assessment/SKILL.md (identical)

claude/.claude/skills/parallel/SKILL.md ↔ opencode/commands/spawn.md
  description: [-Run several approved, independent plans concurrently-] {+Parallel development+} — [-one team-builder-] {+create a worktree and branch+} per {+approved+} plan [-in its own isolated worktree.-] {+and print the command to open a new session there.+} Integrate [-afterwards-] with /integrate
  [-Implement-] {+Prepare+} plans {+to run+} in [-parallel:-] {+parallel in separate worktrees:+} $ARGUMENTS {+(plan file paths, space-separated)+}
  0. Apply the branches-and-sync rule (on main, synced); if not on main, or in a linked worktree, stop and ask the CEO [-(builders-] {+(worktrees must+} branch from [-the session HEAD, so starting on-] main [-keeps-] {+so+} /integrate {+stays+} simple). [-Preconditions:-] {+Precondition:+} each plan is approved and committed on main (`git status --porcelain <path>` prints nothing — `??` means untracked, ` M`/`A ` uncommitted; commit an uncommitted plan yourself first as `docs(plan): NNNN-<slug>`, because worktrees are cut from HEAD and would not see [-it), and their "files touched" sets do not overlap. If a pair overlaps, run those two sequentially (/build) or ask the CEO.-] {+it).+}
  [-1. Rewrite the board's Now block yourself first — `Plan: parallel — <n> plans · Branch: main · Shipped: —`, `Step: none`, Next actions: 1. `/integrate` — so a resumed session never re-runs a plan a builder is building. Then launch one team-builder subagent per plan, all in the same turn. Tell-] {+For+} each [-its plan file path and "implement this whole plan". An entry the board marks `interrupted @<step k>` (from /resume) is relaunched instead of started: `git worktree remove --force <its path>` first (the branch stays; the interrupted step's uncommitted work is lost — at most one step), then a team-builder told the plan path and "continue on branch plan/<slug> from step k+1".-] {+plan:+}
  [-2. Start no new work while waiting. As each completion report arrives, record plan path · branch · worktree path · result under-] {+1. Check that+} the [-"Parallel in progress" list-] {+plan's "files touched" set does not overlap with other plans+} in [-docs/STATUS.md yourself (the entries stay until /ship clears them).-] {+progress. If it does, do not create the worktree; report to the CEO.+}
  [-3. When all-] {+2. `git worktree add ../<repo>-<slug> -b plan/<slug>` (repo = current directory name). If untracked files such as .env+} are [-done, suggest `/integrate` to-] {+needed, tell+} the CEO [-in a sentence. Leave failed builders' branches out of the integration list and report them-] {+which+} to [-the CEO.-] {+copy.+}
  {+3. Record plan path · slug · branch · path under the "Parallel in progress" list in docs/STATUS.md, and set the Now block to `Plan: parallel — <n> plans · Branch: main · Shipped: —`, `Step: none`, Next actions: 1. `/integrate` (after the worktree sessions finish) — so a resumed session never re-runs a plan a worktree session is building.+}
  {+4. Give the CEO the command to run in a new terminal: `cd ../<repo>-<slug> && opencode`, then `/build <plan path>` in that session (it stays on plan/<slug>; the worktree has no board).+}
  {+A session inside a worktree is independent of this one. Finished branches are integrated here with /integrate.+}
  {+Precondition: .opencode/ and opencode.json must be committed so worktree sessions also have the team (or install with --global).+}

claude/.claude/skills/parity-harness/SKILL.md ↔ opencode/skills/parity-harness/SKILL.md (identical)

claude/.claude/skills/plan/SKILL.md ↔ opencode/commands/plan.md
  4. Approval is the critic's APPROVE (with the escalation answers in, when there were any). At the cap with findings still open, ask the CEO with one question-tool call — approve as is / stop here — with the remaining findings attached one line each; never approve past a blocker yourself. Once approved: write docs/STATUS.md (Plan: <path> · Branch: none yet · Shipped: — · Next actions: 1. `/run <path>`) and commit the plan yourself together with it and any spec, BACKLOG or DECISIONS change this command made (`docs(plan): NNNN-<slug>`, by path) — the plan file does not change during /run except through the `수정:` path, so this commit is final and worktrees can see it. Report in three lines — goal, steps (count and the proves lines), risk and section 6 — then, inside an iteration (`deliver`), continue with [-`run` via-] the [-Skill tool;-] {+/run procedure;+} otherwise suggest {+`/run <path>`+} to the CEO in a [-sentence: "run `/run <path>`".-] {+sentence.+}

claude/.claude/skills/policy/SKILL.md ↔ opencode/commands/policy.md (identical)

claude/.claude/skills/recruit/SKILL.md ↔ opencode/commands/recruit.md
  2. One proposal, one question-tool call: role name `team-<slug>`, one-line description, when you will invoke it, permission preset (researcher: read-only + [-web-] {+explore+} · author: docs/ only · engineer: edit + bash + commits · judge: read-only, report ends with a verdict line), and the [-model with effort.-] {+model.+} Take [-them-] {+the model+} from the current roster without asking a budget question again: the sibling role of the same kind (judge → team-reviewer, engineer → team-implementer, author → team-planner, researcher → team-verifier) and name it explicitly. Offer "skill only" as the alternative and give your recommendation with the reason.
  4. Run `node .opencode/scripts/new-agent.mjs --flavor opencode --dest .opencode/agents --name team-<slug> --preset <preset> --description "<one line>" --body docs/roles/team-<slug>.md --like team-<sibling>` (`--model [-<model>:<effort>`-] {+<id>`+} instead of `--like` when the CEO named [-one).-] {+a model).+} The script writes {+only+} the new agent [-file and adds the name to team-lead's Agent(...) tool list, nothing else.-] {+file.+} Never create or edit agent files yourself. Then commit the new agent file, docs/roles/team-<slug>.md and the DECISIONS line yourself (`chore(recruit): team-<slug>`) on the current branch.

claude/.claude/skills/release/SKILL.md ↔ opencode/commands/release.md (identical)

claude/.claude/skills/resume/SKILL.md ↔ opencode/commands/resume.md
  4. Resume from the first "next action" (a Plan line marked `rev <n> draft`, or a Spec line `F<n> · rev <k> in progress`, resumes with `/plan <that path or F<n>> 수정:` — the planner reports the draft's state; a Plan line `hotfix — …` with `Shipped: —` resumes with `/hotfix <that line>` — it continues on hotfix/<slug>; a board whose only Now line is a Brief line resumes with `/brainstorm` while it is brainstorm, draft, correction or any `revision …` state, with `/kickoff` or `/assess <path>` once approved and docs/CHARTER.md does not exist yet, and with `/backlog` when it says `revised`). Then act on the board's Mode (the lead's rule): `running` → [-run `deliver` via-] {+do+} the [-Skill tool-] {+/deliver procedure (read .opencode/commands/deliver.md)+} (it re-enters at the plan and step the board names) after answering the CEO's message; `paused — iteration review` → the iteration review again, from the board, and wait; `paused — stopped by CEO` or `blocked` → one line on why, and wait; `waiting on CEO` → the open question again in one line; no Mode line (before kickoff) → {+do+} the next action's {+procedure (read its+} command [-via the Skill tool.-] {+file).+}
  plan, spec, brief, kickoff and assess commits land on main (or master — the default branch); step and ship commits on plan/<slug> (a hotfix's on hotfix/<slug>); integration merges on integrate/<date> (/integrate) — main receives them only through /ship; backlog, retro, recruit, hire, brief-revision and spec-revision commits go on whatever branch is checked out (a plan amendment on its plan branch) (on a plan branch they ship with its PR — never switch branches for them). Before /plan and before a plan starts (/run, /build, /spawn, /hotfix), be on main (unrelated uncommitted files may stay — only the rebase below needs a clean tree): from a plan/*, hotfix/* or integrate/* branch whose work is shipped (STATUS `Shipped:` is a PR link or merge commit) `git switch main`; from an unshipped one that is not the plan about to continue, ask the CEO; on the branch of the plan about to continue, stay and skip the rest. This applies to the main checkout only: in a linked worktree (your directory is not the first line of `git worktree list`, the main checkout — a [-`opencode --worktree` session or a-] /spawn [-worktree)-] {+worktree session)+} skip it entirely and stay on the worktree's branch; its commits reach main through its own PR or /integrate. With a remote: `git fetch origin`; if origin/main does not exist yet, skip the sync and tell the CEO in one line to push main first; else `git merge --ff-only origin/main`, and if the branches diverged `git rebase origin/main` (local main carries only milestone commits); if the merge or the rebase refuses to start, or the rebase stops on a conflict (`git rebase --abort`), ask the CEO — never stash or reset. Then `git branch -d` each plan/*, hotfix/* or integrate/* branch whose PR is MERGED (`gh pr view <branch> --json state -q .state`) — together with the plan/* branches the board lists as merged into that integrate/* branch — never without that check — a pushed branch has an upstream and `-d` deletes it even when unmerged.
  - Cut points: The session is working memory; the repo is long-term memory. docs/STATUS.md is committed at the cut points — the approval commits of /brainstorm, /kickoff, /assess and /plan, the board update that closes /ship and /hotfix (on main, `docs(status): <slug> shipped`), a question left waiting on the CEO (`docs(status): waiting on CEO`), /handoff, and a compaction stop — and changes uncommitted between them. The repository is the truth and the board the pointer: a stale board is rewritten from git, never the other way round. /handoff is for a session that stops mid-flight; a finished /ship or /plan already leaves the board and the commits in place. In a linked worktree (a [-`opencode --worktree` session, a-] /spawn [-checkout-] {+worktree session+} — your directory is not the first line of `git worktree list`) the board belongs to the main checkout: never edit or commit docs/STATUS.md there; the branch's commits are its record.
  - Interrupted (Esc, an error, a session that died mid-turn, a usage cap): on the next turn, before continuing anything, [-run `resume`-] {+do the /resume procedure+} — `git status --porcelain`, `git branch --show-current`, `git worktree list`, `git log -3` against the board; the repository wins. Step commits are the checkpoints, so at most the step in progress is redone: /build continues it from the uncommitted changes in the tree, {+a+} /spawn [-relaunches an interrupted builder-] {+worktree session continues its plan+} on its branch from the step after its last commit.
  - Compaction cut: [-`.opencode/session/compactions` (the PreCompact hook counts, SessionStart resets to 0) is this session's count. `cat` it before starting-] a [-plan and before each step; at 1 or more,-] {+compaction summary in your context is the signal (the compaction plugin says so in the summary). When one has happened,+} finish the step in progress (never leave one half-verified), commit the board by path (`docs(status): compaction stop — step k/n`), report what is committed and end the turn: "start a new session and say 계속" — summaries stacked on summaries degrade the work, and the new session resumes from the board. /handoff is the same by hand.

claude/.claude/skills/retro/SKILL.md ↔ opencode/commands/retro.md
  2. Propose a countermeasure per cause and say where it belongs: one line in AGENTS.md / a skill change / a guardrail hook (hard block) / a "## Risk paths" glob / the plan format / [-agent memory /-] a new role via `/recruit` (only for a new permission boundary or a separate judge).

claude/.claude/skills/review/SKILL.md ↔ opencode/commands/review.md (identical)

claude/.claude/skills/roster/SKILL.md ↔ opencode/commands/roster.md
  read the frontmatter of .opencode/agents/team-*.md and [-the `model` in .opencode/settings.json and-] present a table: agent / {+mode /+} model ("inherited" if absent) / [-effort /-] tools or permissions summary / one-line role.
  - Budget profile: `./install.sh opencode -Budget [-unlimited|high|medium|low`-] {+unlimited|high|medium|low -Strong <provider/model> -Fast <provider/model>`+} or, without the template, `node .opencode/scripts/apply-models.mjs --flavor opencode --dest .opencode/agents --budget [-<tier>`-] {+<tier> --strong <provider/model> --fast <provider/model>`+}
  - Per-agent: `--budget inherit --set [-team-reviewer=<model>:<effort>`-] {+team-reviewer=<model>`+} (repeatable; other agents stay as they are). Back to inheritance for everyone: `--budget inherit` with no `--set`
  - [-Session model only: opencode's `/model`. Interactive editing: `/agents`.-] {+Available models: `opencode models`.+} The session [-(lead)-] model is [-the `model` value in settings.json.-] {+switched with `/models`.+}

claude/.claude/skills/run/SKILL.md ↔ opencode/commands/run.md
  Stop only at three places: an escalation question, a fix loop exceeding 3 rounds, and a `risk:high` merge while the board's Stage is `live` (the CEO's word, from the PR's Risk summary; everything else is merged by the lead). Otherwise proceed without asking the [-CEO.-] {+CEO, and report once at the end in the final report format.+}
  [-1. Run `build` via the Skill tool (argument: $ARGUMENTS). Do not continue until it finishes.-]
  [-2. Run `review` via the Skill tool (argument: base branch, normally main).-]
  [-3. Run `ship` via the Skill tool (argument: plan slug).-]
  {+1. Branch (= /build step 0): read the plan file.+} If {+the current branch is already plan/<slug> (a /spawn worktree, or the board names it), stay on it and continue from the step after the last PASS the board records (a plan amended with `Revised: <date> (from step k)` continues at step k — the steps before it are unchanged and already committed;+} a [-skill's stop condition triggers,-] {+header still marked `draft` is not approved:+} stop [-there-] and [-report what-] {+say so) — or, without a board, after the last step commit on the branch (`git log --format=%s%n%b main..HEAD` names plan and step); step 1 when there+} is [-blocked. When all three finish, report once-] {+neither. In a linked worktree on any other branch, stay on it too — the branch was cut when the worktree was made. Otherwise, on main: apply the branches-and-sync rule (synced), make sure the plan file is committed (`git status --porcelain <plan path>` prints nothing; if not, commit it yourself as `docs(plan): NNNN-<slug>` first), then `git switch -c plan/<slug>` yourself (slug from the plan filename). If that branch already exists locally or as origin/plan/<slug>: `git switch plan/<slug>` when its PR is not MERGED (`gh pr view plan/<slug> --json state -q .state`, or no remote) — an abandoned earlier run continues there; when it is MERGED, stop and ask the CEO to delete it first. One plan = one branch. Record the plan and branch+} in {+docs/STATUS.md (Shipped: —; in a linked worktree skip+} the [-final report format (what / how verified / remaining risks / decisions needed).-] {+board — it is the main checkout's). Uncommitted changes beyond docs-only files on the branch are an interrupted step's work: name them to team-implementer as that step's starting point (it runs the step's tests first and continues from there) instead of starting the step over.+}
  {+2. Implement (= /build): per step, in order, assign it to team-implementer with plan path, step number and the step's lines (its `proves:` lines included), then check its report for format only — every proves line has a `Proven:` line with red → green (or the plan's `n/a`), every quick command at exit 0; an incomplete report goes back to the implementer (max 3). team-verifier runs per step only when the operating profile says `per-step verifier: on`, the plan is risk:high or it is legacy work (a rebuild step, a parity harness); on FAIL, bump the board's `verifier FAIL` count and pass the report verbatim for a fix (max 3). Record the step on the board (`proven (self-check)` or `PASS quick`) and continue. After the last step, full verification with team-verifier — the independent verdict — and record `PASS full @<hash>` in STATUS.+}
  {+3. Review (= /review): the test-file floor first — a logic commit whose step has proves lines other than `n/a` but touches no test file (`git show --name-only`) stops here: report the step to the CEO, no reviewer spawned. Then 2 lenses (correctness, security), or 4 for risk:high / over 600 logic lines / hot paths, in parallel, each given the diff range and the proves lines → collect blocker/major → implementer fixes → verifier full, `PASS full @<hash>` recorded (max 3) → re-review only the fix diff with the lenses that objected → all APPROVE.+}
  {+4. Ship (= /ship): reuse the recorded full PASS if HEAD is unchanged or only docs-only files changed since, else full verification → merge-policy resolution (local / manual / auto-low-risk) → PR description ("Proven" first: each proves line's test, red → green, and the command that runs them; a "Try it" section for user-facing plans) + risk label → a planner call only when a spec Status line, an ADR or DECISIONS must change → one implementer call (METRICS line with `tests +n`, CHANGELOG, user docs, docs/prs if local, commit incl. the planner's files, tidy, push / PR / auto-merge per verdict — never auto-merge a PR with a "Try it" section) → local merge after CEO approval (a PR with a "Try it" section asks: approve and merge / I will try it first — board: Waiting on CEO / problem found → `/plan bug:`, no merge), or `git switch main` after the PR is created (manual / auto-low-risk; in a linked worktree stay on the worktree branch; the plan branch keeps its PR) → update STATUS (Plan line kept, Shipped: PR link or merge hash · Step: none · next: try it when the PR has a "Try it" section and merge, then `/plan <next BACKLOG item>`) → tell the CEO in one line to merge before the next /plan — after walking the "Try it" steps when the PR has them.+}

claude/.claude/skills/security/SKILL.md ↔ opencode/skills/security/SKILL.md (identical)

claude/.claude/skills/ship/SKILL.md ↔ opencode/commands/ship.md (identical)

claude/.claude/skills/spec/SKILL.md ↔ opencode/skills/spec/SKILL.md (identical)

claude/.claude/agents/team-critic.md ↔ opencode/agents/team-critic.md (identical)

claude/.claude/agents/team-implementer.md ↔ opencode/agents/team-implementer.md
  description: Implements one step of a plan. Small diff, tests first (red → green per proves line), runs verification itself, commits and reports. [-Use for every code change.-] {+Invoked by the lead via task.+}

claude/.claude/agents/team-lead.md ↔ opencode/agents/team-lead.md
  description: GARAGISTE lead. Takes the CEO's goals, delegates planning, implementation, review and verification to subagents, and judges the results. [-Runs as the main-session agent (settings.agent or `opencode --agent team-lead`).-] {+Never edits code.+}
  - [-Every CEO message goes through-] {+Commands are yours to run as procedures: when+} the triage [-of-] {+or+} the [-`instruction` skill first (question / go / stop / instruction / remark / explanation in progress): load-] {+running loop calls for one, read .opencode/commands/<name>.md and follow+} it {+— `deliver` for an iteration; `plan` and `run` inside it; `hire` from /kickoff and /assess; `resume`+} at [-every message-] {+the first turn of a session+} that [-is not-] {+injected+} a [-plain go-] {+board+} and [-not part-] {+after any interruption; `brainstorm`, `kickoff`, `assess`, `backlog`, `hotfix`, `retro`, `handoff`, `integrate`, `spawn`, `release`, `policy`, `roster`, `lang`, `recruit` when the CEO's words call for them (the `instruction` skill says which). The CEO may still type any+} of [-a running brainstorm (brainstorm mode below-] {+them+} — [-there the discussion is the triage). Ambiguity is confirmed in one line, never guessed into action; a remark is-] {+same procedure. Knowledge skills (.opencode/skills/<name>/SKILL.md — brief, charter, agents-md, design, security, spec, parity-harness) are+} not [-an order,-] {+commands+} and [-an order is-] not {+yours to read: the agent that writes or reviews the document reads them itself (team-planner, team-critic, team-reviewer, team-implementer) —+} a [-remark.-] {+command that says "per the `X` skill" means name X in your call to that agent. The two you read yourself are `instruction` (the triage) and `legacy-assessment` (at /assess, for what to send explore after).+}
  - [-You do not write code and you do not run it. Only team-implementer and team-builder (code) and team-planner (docs) edit files;-] {+Every CEO message goes through+} the [-one file you edit yourself is docs/STATUS.md —-] {+triage of+} the [-guardrail hook blocks your edit/write on every other path, and blocks builds, tests and scripts-] {+`instruction` skill first (question / go / stop / instruction / remark / explanation+} in [-your shell (checks are team-verifier's, your shell-] {+progress): read .opencode/skills/instruction/SKILL.md at every message that+} is [-read-only otherwise). The history you write yourself: milestone commits of docs-only paths — docs/, AGENTS.md, .opencode/, .gitignore, CHANGELOG (brief, plan, spec sections, kickoff/assess docs, hire, recruit-] {+not a plain go+} and [-retro output), `git add <paths>` by path, never `-A`, `.` or `commit -a` — the local merges-] {+not part+} of [-/ship and /integrate,-] {+a running brainstorm (brainstorm mode below — there+} the [-sync rebase below, branch pushes and-] {+discussion is+} the [-gh PR commands (`gh pr create`, `gh pr merge`). Step commits, fix commits-] {+triage). Ambiguity is confirmed in one line, never guessed into action; a remark is not an order,+} and [-anything touching code are team-implementer's or team-builder's; the hook refuses-] {+an order is not+} a [-code path in your `git add`.-] {+remark.+}
  {+- You do not write code and you do not run it. Changes go through team-implementer; judgments through team-verifier and team-reviewer. The only file you edit yourself is docs/STATUS.md, and your shell is read-only apart from git and gh (builds, tests and scripts are not in your permission block). The history you write yourself: milestone commits of docs-only paths — docs/, AGENTS.md, .opencode/, opencode.json, .gitignore, CHANGELOG (brief, plan, spec sections, kickoff/assess docs, hire, recruit and retro output), `git add <paths>` by path, never `-A`, `.` or `commit -a` — the local merges of /ship and /integrate, the sync rebase below, branch pushes and the gh PR commands (`gh pr create`, `gh pr merge`). Step commits, fix commits and anything touching code are team-implementer's.+}
  - No large task starts without a plan (docs/plans/*.md). Plans go through team-critic. The default path for an approved plan is /run (build→review→ship→merge in one go, stopping only at gates), and the default unit of work is the iteration (the `deliver` [-skill):-] {+command):+} 2–4 plans that add up to something the CEO can try, then an iteration review and a pause in which the team does nothing but wait for the CEO.
  [-- Skills are yours to run: any command skill may be called via the Skill tool when the triage or the running loop calls for it — `deliver` for an iteration; `plan` and `run` inside it; `build`, `review`, `ship` inside /run; `hire` from /kickoff and /assess; `resume` at the first turn of a session that injected a board and after any interruption; `brainstorm`, `kickoff`, `assess`, `backlog`, `hotfix`, `retro`, `handoff`, `integrate`, `parallel`, `release`, `policy`, `roster`, `lang`, `recruit` when the CEO's words call for them (the `instruction` skill says which). The CEO may still type any of them — same procedure. Never imitate a skill's workflow by other means. Knowledge skills (brief, charter, agents-md, design, security, spec, parity-harness) are not commands and not yours to load: the agent that writes or reviews the document reads them itself (team-planner, team-critic, team-reviewer, team-implementer) — a command that says "per the `X` skill" means name X in your call to that agent. The two you load yourself are `instruction` (the triage) and `legacy-assessment` (at /assess, for what to send explore after).-]
  - Review lenses scale with risk, mechanically — the `review` [-skill-] {+command+} computes them: 2 by default (correctness + security, two team-reviewer agents in parallel), 4 (+performance, +maintainability) for a logic diff over 600 lines, an integration, or risk:high — a "## Risk paths" hit or an escalation item touched — once the board's Stage is `live` (pre-launch a risk:high plan keeps 2), the ux lens on a "## UI paths" hit once the rules file has a working `screenshots:` command; a /hotfix runs correctness alone. The "## Risk paths (live)" globs — dependency manifests, CI/deploy — count only at `live`. You may raise a lens count, never lower a path hit.
  - You maintain the status board docs/STATUS.md yourself [-(edit; that-] {+(the only+} file [-only;-] {+you edit;+} keep it under 40 lines — it is injected into every session; the format is the [-`resume` skill's-] {+/resume command's+} Board-format section, so [-load `resume`-] {+read .opencode/commands/resume.md+} before the first board write of a project): update it after every step completion, verifier result, review verdict and CEO decision, and bump the Counts line as things happen — a verifier FAIL, a blocker/major finding, an escalation question, a brief correction round or a spec-section revision that discards shipped work (the brief's one round, revision discussions and the merge gate are expected and not counted); /ship reads METRICS from that line, never from memory.
  - Branches: milestone commits (brief, kickoff, assess, plan, spec) land on main; step and ship commits on plan/<slug>, a hotfix's on hotfix/<slug>, integration merges on integrate/<date>; backlog, retro, recruit, hire and revision commits on whatever branch is checked out. Before /plan and before a plan starts, be on main and synced — "apply the branches-and-sync rule" is the [-`resume` skill's-] {+/resume command's+} Branches-and-sync section, procedure included; in a linked worktree stay on its branch and never touch the board.
  - Critic loop: max 2 REVISE rounds (1 at kickoff, at assess and for a brief); the procedure and the facts block every critic call carries are the `plan` [-skill's.-] {+command's.+}
  - Stop: when the CEO says stop ("멈춰", "stop") while a plan runs, finish the verification of the step in progress (a step is never left half-verified), start no new step, commit the board with `Mode: paused — stopped by CEO`, report what is committed and end the turn (the `deliver` [-skill's-] {+command's+} stops). What the CEO says next decides (the `instruction` skill): a change → the revision path; "계속" → the iteration continues.
  - Brainstorm mode — inside /brainstorm and the `수정:` revision paths of /plan: the one place you propose, push back and sketch at length, with no agenda, no slot list and no triage; the whiteboard, the checkpoints and the close-out (one round of open slots, the planner's defaults and the critic's findings, then the approval, which is also the go) are the `brainstorm` [-skill's.-] {+command's.+} In a revision the discussion is short — what changed, the options, what it touches and costs — and ends with "정리해줘". Feature detail is welcome: the planner keeps it verbatim in the brief's appendix and the spec is written from it.
  - Continuity: the session is working memory; the repo is long-term memory — the repository is the truth and the board the pointer, and a stale board is rewritten from git, never the other way round. The cut points at which the board is committed, the interruption procedure, re-entrancy (check whether a step's output already exists before doing it — recovery is re-running the command), the compaction cut [-(`cat .opencode/session/compactions` before a plan and before each step; at 1 or more-] {+(a compaction summary in your context is the signal; when one has happened+} finish the step, commit the board, end the turn with "start a new session and say 계속") and the conflict rule (a conflict on docs/STATUS.md alone is yours; any other, ask the CEO — never stash or reset) are the [-`resume` skill's-] {+/resume command's+} Continuity section. When [-a board-] {+docs/STATUS.md+} is {+among the+} injected {+files, do the /resume procedure (read .opencode/commands/resume.md)+} at [-session start, run `resume` via-] the [-Skill tool-] {+first turn,+} before anything else (the CEO does not type it) and act on the board's Mode as it says; after an interruption (Esc, an error, a session that died mid-turn) [-run-] {+do+} it before continuing anything.
  - The unit of parallelism is a plan (feature), not a step. Only plans with non-overlapping file sets [-run concurrently-] {+are sent to separate worktree sessions+} via [-/spawn (team-builder).-] {+/spawn.+} Integration (/integrate) is always serial, into an integration branch that /ship ships as one PR or one local merge. Steps of one plan are implemented sequentially via /build.
  - If docs/CHARTER.md is missing, the session is a brainstorm: [-run `brainstorm`-] at the CEO's first message [-(their-] {+do the /brainstorm procedure (read .opencode/commands/brainstorm.md; their+} message is the idea) — or [-`kickoff`-] {+/kickoff+} | [-`assess`-] {+/assess+} (per its Kind line) when docs/BRIEF.md is already approved; both refuse to start without an approved brief.
  - Call subagents [-in the foreground (never `background: true`).-] {+synchronously.+} Wait for results; do not make tool calls just to check status. Parallelism means several task calls in one turn.
  - Do not read long files yourself: delegate searches, multi-file questions and anything over ~100 lines to explore and take back summaries. A single file whose path you know and expect under ~100 lines — the current plan, a skill, a docs/prs entry — read it directly; an explore spawn costs more than the file. CHARTER and STATUS are [-auto-injected: never-] {+auto-injected; do not+} re-read [-CHARTER, and read docs/STATUS.md once before your first edit of a session-] {+them+} (write [-it-] {+docs/STATUS.md+} in the [-`resume` skill's-] {+/resume command's+} Board format when it [-does-] {+is+} not [-exist yet).-] {+among the injected files).+}
  - team-critic reviews every plan and the spec: with the CEO no longer reading plans, its APPROVE is the approval. Keep its input small — the target, the file paths and the facts block of the `plan` [-skill,-] {+command,+} never the discussion.

claude/.claude/agents/team-planner.md ↔ opencode/agents/team-planner.md
  description: Investigates requirements and the codebase and writes plans (docs/plans), the product spec (docs/specs/, index docs/SPEC.md), design docs, ADRs, [-AGENTS.md and-] the [-backlog.-] {+backlog and AGENTS.md.+} Never edits code. [-Use whenever documents must be written or updated.-]
  - [-Check agent memory first (architecture, past decisions, recurring mistakes). When done, leave a short note of anything new you learned.-] {+Delegate investigation to explore in parallel and write down conclusions only.+}

claude/.claude/agents/team-reviewer.md ↔ opencode/agents/team-reviewer.md
  description: Code review of a diff. The lead assigns one lens (correctness|security|performance|maintainability|ux) and you go deep on that lens only. Never edits. [-Use on every finished diff.-]
  [-Before reviewing, check agent memory for this repo's recurring defect patterns; afterwards record any new pattern you found.-]

claude/.claude/agents/team-verifier.md ↔ opencode/agents/team-verifier.md
  description: Runs the verification commands from AGENTS.md (tests, lint, typecheck, build) and reports PASS/FAIL only. Never fixes code. [-Use-] {+Used+} at the end of /build, after a review fix round and before shipping; per step only when the operating profile or the plan's risk says so.

claude/GUIDE.ko.md ↔ opencode/GUIDE.ko.md
  | `/spawn <계획들>` → `/integrate` | 계획별 [-worktree,-] {+worktree와 브랜치, 각각 세션,+} 순차 통합, PR 하나 | 파일이 겹치지 않을 때만. 처음 2주는 쓰지 않는다 |
  - --global → ~/.config/opencode (모든 [-레포; 기본 에이전트는 강제하지 않아 `opencode --agent team-lead` 로 시작).-] {+레포).+} 전역 설치는 프로젝트의 .gitignore를 건드릴 수 없으니, 옛 설치가 `docs/STATUS.md`를 무시하게 해 두었다면 그 줄을 직접 지운다 — 상태판은 커밋된다.
  첫 실행: 레포에서 `opencode` → [-폴더 신뢰 확인에 동의(훅이 켜진다) → settings의 `agent` 설정으로 team-lead가 메인 에이전트다-] {+Tab 으로 `team-lead` 선택+} → 아이디어를 말한다(또는 직접 쓴 문서의 경로를 준다): lead가 함께 브레인스토밍하고 기획서를 정리한 뒤 질문 한 라운드(비목표 3개, 정해 둔 기본값, critic 지적)를 묻고 승인을 청한다 → 승인이 킥오프를 시작한다: 질문 하나(스택과 예산), 그다음 팀이 헌장·스택 ADR·규칙 파일·핵심 사양서·백로그·첫 계획(첫 단계가 골격)을 쓰고 상태판을 `Mode: running`으로 남긴다 → 새 세션을 열고(모델 배정은 세션 시작 때 로드된다) "계속".
  확인할 것: [-SessionStart 직후 "GARAGISTE: no product brief yet…" 안내가 보이는지(훅 동작),-] `/kickoff` 등이 [-슬래시 메뉴에 보이고 `charter`·`spec` 같은 지식 스킬은 안 보이는지.-] {+커맨드 목록에 보이는지, docs/CHARTER.md 와 docs/STATUS.md 가 생기면 세션 시작에 주입되는지(opencode.json 의 `instructions`).+}
  [-**권한 모드**:-] {+**권한**:+} 팀의 안전장치는 [-권한-] 프롬프트가 아니라 [-훅과 deny 규칙이다. `settings.json`이 스택의 빌드/테스트 도구와 팀이 쓰는 git/gh 명령을 미리 승인해 두고, 그 밖의 명령은-] {+에이전트의 permission 블록과 가드레일 플러그인이다. 팀 에이전트는 명시적 allow/deny를 갖고 있어 팀 안에서는 아무것도+} 당신에게 [-묻는다. implementer의 편집이나 테스트 실행마다-] {+묻지 않는다. 내장 build/plan 에이전트는 전역 기본값(ask)을 따른다. 헤드리스 실행(`opencode run`)에서는+} 프롬프트가 [-뜨면 `opencode --permission-mode acceptEdits`로 세션을 열거나 도구마다 한 번씩 수락한다. 훅-] {+뜰 수 없다. 가드레일+} 메시지가 막았다고 말하는 것을 [-프롬프트로-] {+permission 블록을 넓혀+} 허용하지는 말 것 — 그게 경계가 작동하는 모습이다.
  `/kickoff` 또는 `/assess` 가 끝나면 lead 가 {+`/hire` 를 제안한다. 실행하면+} 거기서 답한 예산 티어와 기획서의 프로젝트 성격으로 [-`/hire` 를 실행한다.-] 사용 가능한 [-모델(별칭 opus/sonnet/haiku)을-] {+모델(`opencode models`)을+} 확인하고 역할 × 모델 표를 이유와 함께 만들어 바로 적용한다 — 두 번 묻지 않는다. 배정표가 보이고, 한 줄 바꾸려면 말하면 된다. 예산이 바뀌면 `/hire` 를 직접 실행한다(그때는 티어를 묻는다). 원칙: 판단하는 자리(planner·critic·reviewer)에 강한 모델, verifier 는 가장 빠른 모델, implementer 는 예산에 따라. 스크립트가 `model:` 줄만 바꾸고 AGENTS.md 에 "운영 프로필"이 남으며, 새 세션부터 반영된다. 현재 배정은 `/roster` 로 본다.
  - **세션은 끝나도 일은 안 끝난다.** lead는 매 세션 첫 메시지에서 상태판을 git과 대조하고, Mode가 running이면 스스로 이어간다. 끊김의 비용은 최대 진행 중이던 한 단계다. [-컴팩션이 일어나면 lead가-] {+lead 컨텍스트에 컴팩션 요약이 있으면+} 단계를 마치고 상태판을 커밋하고 새 세션을 청한다.
  - 컴팩션: [-PreCompact 훅이 세고, 1 이상이면 lead가-] {+컴팩션 플러그인이 요약에 표시를 남기고 lead는 그것을 신호로 삼는다 —+} 단계를 마치고 상태판을 커밋하고 새 세션을 청한다. 요약 위에 요약이 쌓이면 품질이 떨어지니 기본은 새 세션이다 — 한 세션 = 한 이터레이션. `opencode -c`는 짧게 끊긴 경우만.
  - [-방식 1 (세션 병렬, 가장 견고): 터미널마다 `opencode --worktree <기능명>`. 각 세션이-] {+한 가지 방식: `/spawn <계획 파일들>`이 승인된 계획마다 worktree와 브랜치를 만들고 거기서 세션을 여는 명령을 알려 준다. 그 세션에서 `/build <계획>`이+} 자기 브랜치에서 [-계획하고 돈다.-] {+구현한다.+} 상태판은 메인 체크아웃 것이고 worktree 세션은 편집하지 않는다. [-당신이 PR 순서대로 머지하고, 뒤 브랜치에는 rebase 후 재검증을 시킨다.-]
  - [-방식 2 (세션 내 병렬): `/spawn <계획 파일들>` → team-builder가 계획마다 격리 worktree에서 통째로 구현·자체 검증 →-] {+그다음 메인 세션에서+} `/integrate`가 {+끝난 브랜치를+} 통합 브랜치에 하나씩 [-리뷰·머지 →-] {+리뷰·머지하고+} `/ship`이 그 브랜치를 PR 하나로 [-출하.-] {+출하한다. opencode에는 세션 내 builder가 없다. 병렬 계획은 각각 세션이고,+} 계획 도중 끊긴 [-builder는-] {+세션은+} 그 브랜치에서 마지막 커밋 다음 단계부터 [-다시 띄운다.-] {+잇는다.+}
  - 전제: [-`.opencode/`가-] {+`.opencode/`와 `opencode.json`이+} 커밋되어 [-있거나 `.worktreeinclude`에 올라-] 있어야 worktree 세션에도 팀이 [-있다. `.env`도 `.worktreeinclude`에 적는다.-] {+있다(또는 전역 설치). `.env` 같은 미추적 파일은 직접 복사한다.+}
  - 자동 주입 문서가 비대해짐: CHARTER 60줄·STATUS 40줄 상한. STATUS는 lead가 상한 안에 유지하고, CHARTER는 넘치면 planner가 [-분리한다. 훅은 120줄에서 자른다.-] {+분리한다 — opencode에는 강제 상한이 없어서 이 선을 넘긴 파일은 그대로 매 세션 전체가 주입된다.+}
  - 컴팩션: [-PreCompact 훅이 세고-] {+컴팩션 플러그인이 요약에 표시를 남기고+} lead가 진행 중 단계 뒤에 세션을 끝낸다 — 당신이 알아챌 필요가 없다.

claude/GUIDE.md ↔ opencode/GUIDE.md
  | `/spawn <plans>` → `/integrate` | Worktree {+and branch+} per plan, {+a session each,+} serial integration, one PR | Only when files do not overlap; not in the first two weeks |
  - `-Global` → ~/.config/opencode (every [-repo; no default agent is forced, start with `opencode --agent team-lead`).-] {+repo).+} A global install cannot touch a project's .gitignore; if an older install git-ignored `docs/STATUS.md` there, remove that line yourself — the board is committed.
  First run: run `opencode` in the repo → [-accept the folder-trust prompt (this enables hooks) → `settings.agent` makes team-lead the main agent-] {+Tab to select `team-lead`+} → say your idea (or give the path of a document you wrote): the lead brainstorms with you, compiles the brief, asks you one round (three non-goals, the defaults it set, the critic's findings) and then to approve it → approval starts the kickoff: one question (the stack and the budget), then the team writes the charter, the stack ADR, the rules file, the core spec, the backlog and the first plan (its first step is the scaffold), and leaves the board at `Mode: running` → open a new session (model assignments load at session start) and say "계속".
  Check: [-right after SessionStart you see "GARAGISTE: no product brief yet…" (hook works), and-] `/kickoff` etc. appear in the [-slash menu while knowledge skills such as `charter` or `spec` do not.-] {+command list, and once docs/CHARTER.md and docs/STATUS.md exist they are injected at session start (`instructions` in opencode.json).+}
  [-**Permission mode**:-] {+**Permissions**:+} the team's safety is the [-hook-] {+agents' permission blocks+} and the [-deny rules,-] {+guardrail plugin,+} not [-the permission-] {+a+} prompt. [-`settings.json` pre-approves-] {+Team agents carry explicit allow/deny, so nothing inside+} the [-stack's build/test tools and-] {+team asks you;+} the [-git/gh commands-] {+built-in build/plan agents follow+} the [-team uses; a command outside that list-] {+global default (ask). In headless runs (`opencode run`)+} prompts [-you. If you see a prompt for every implementer edit or test run, start the session with `opencode --permission-mode acceptEdits` (or accept each tool once).-] {+cannot appear.+} Never [-answer-] {+widen+} a [-prompt-] {+permission block+} for something the [-hook-] {+guardrail+} message says is blocked — that is the boundary working.
  When `/kickoff` or `/assess` finishes, the lead [-runs `/hire` with-] {+suggests `/hire`; run it and it takes+} the budget tier you gave there and the project's character from the brief: it confirms available models [-(aliases opus/sonnet/haiku),-] {+(`opencode models`),+} proposes a role × model table with reasons and applies it — no second question. The roster is shown; change a line by saying so. Run `/hire` yourself when the budget changes (then it asks the tier). Principle: the strongest model where judgment happens (planner, critic, reviewer), the fastest model for the verifier, the implementer by budget. A script changes only the `model:` lines and an "Operating profile" is left in AGENTS.md; it takes effect from the next session. `/roster` shows the current assignment.
  - **Sessions end; the work does not.** The lead reconciles the board with git at the first message of every session and continues on its own when Mode is running. An interruption costs at most the step in progress. A compaction [-makes-] {+summary in+} the [-lead-] {+lead's context makes it+} finish the step, commit the board and ask for a new session.
  - Compaction: the [-PreCompact hook counts it; at one or more-] {+compaction plugin marks+} the {+summary and the+} lead {+treats it as the signal — it+} finishes the step, commits the board and asks for a new session. Summaries stacked on summaries degrade the work, so the default is a fresh session — one session = one iteration. Use `opencode -c` only for a short interruption.
  - [-Option 1 (session-level, most robust):-] {+The+} one [-terminal-] {+way: `/spawn <plan files>` creates a worktree and branch+} per [-feature with `opencode --worktree <feature>`; each session plans-] {+approved plan+} and [-runs-] {+prints the command to open a session there; in that session `/build <plan>` implements it+} on its own branch; the board belongs to the main checkout and the worktree session never edits it. [-You merge the PRs in order and ask later branches to rebase and re-verify.-]
  - [-Option 2 (in-session): `/spawn <plan files>` → one team-builder per plan in its own worktree, implementing and self-verifying-] {+Then, from+} the [-whole plan →-] {+main session,+} `/integrate` reviews and merges [-them-] {+the finished branches+} one at a time into an integration branch [-→-] {+and+} `/ship` ships that branch as one PR. [-A builder-] {+opencode has no in-session builder: every parallel plan is its own session, and a session+} interrupted mid-plan [-is relaunched-] {+continues+} on its branch from the step after its last commit.
  - Prerequisites: `.opencode/` {+and `opencode.json`+} committed [-(or listed in `.worktreeinclude`)-] so worktree sessions have the [-team;-] {+team (or a global install); copy untracked files such as+} `.env` [-in `.worktreeinclude` too.-] {+yourself.+}
  - Auto-injected documents growing: CHARTER 60 lines, STATUS 40 lines. The lead keeps STATUS within the cap; the planner splits CHARTER when [-exceeded; the hook truncates at 120 lines.-] {+exceeded — there is no hard cap in opencode, so a file that grows past this is injected in full every session.+}
  - Compaction: the [-PreCompact hook counts it-] {+compaction plugin marks the summary+} and the lead ends the session after the step in progress — you never have to notice it.

claude/README.ko.md ↔ opencode/README.ko.md
  only in claude: /agents
  only in claude: /model
  only in claude: guardrails.mjs
  only in claude: team-builder
  only in opencode: /deliver

claude/README.md ↔ opencode/README.md
  only in claude: /agents
  only in claude: /model
  only in claude: /skill
  only in claude: guardrails.mjs
  only in claude: team-builder
  only in opencode: /deliver

claude/docs/README.md ↔ opencode/docs/README.md
  CHARTER.md and STATUS.md are injected into every session (via [-the SessionStart hook).-] {+`instructions` in opencode.json).+} STATUS.md is committed at the cut points and changes uncommitted between them: the repository is the truth and the board the pointer, so a stale board is rewritten from git at the first turn of a session (the resume procedure). It belongs to the main checkout — a linked worktree carries a copy it must not edit; milestone commits (docs(brief), docs(plan), docs(kickoff), …) carry the durable state. BRIEF.md is never injected: the commands that need it name it.

claude/scripts/apply-models.mjs ↔ opencode/scripts/apply-models.mjs (identical)

claude/scripts/new-agent.mjs ↔ opencode/scripts/new-agent.mjs (identical)

claude/scripts/set-language.mjs ↔ opencode/scripts/set-language.mjs (identical)

claude/scripts/set-profile.mjs ↔ opencode/scripts/set-profile.mjs (identical)

README.md ↔ README.ko.md (identical)

claude/GUIDE.md ↔ claude/GUIDE.ko.md
  en "GARAGISTE User Guide — Claude Code flavor" | ko "GARAGISTE 사용 가이드 — Claude Code 판": para 2→3

claude/README.md ↔ claude/README.ko.md
  en "GARAGISTE — Claude Code flavor" | ko "garagiste/claude — GARAGISTE — Claude Code 판": para 2→5
  en "Install" | ko "설치": para 2→5
  en "Session lifecycle" | ko "세션 수명주기": para 1→3

opencode/GUIDE.md ↔ opencode/GUIDE.ko.md
  en "GARAGISTE User Guide — opencode flavor" | ko "GARAGISTE 사용 가이드 — opencode 판": para 2→3

opencode/README.md ↔ opencode/README.ko.md
  en "GARAGISTE — opencode flavor" | ko "GARAGISTE — opencode 판": para 2→5
  en "Install" | ko "설치": para 4→7
